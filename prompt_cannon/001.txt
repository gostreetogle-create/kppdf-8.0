10 responses ready
•
33s
Smart Poll

Smart Merge

Smart Sort

GPT-4o Mini icon
GPT-4o Mini (OpenAI)#1

12.7s
Для начала, давайте подробно проанализируем предоставленную информацию и то, что было найдено. Я пройдусь по вашим запросам и оценю, достаточно ли у нас информации для принятия решения о дообучении модели через Soup.

1. Проверка актуальности кода
general.md: необходима проверка текущего состояния файла. Если он все еще является черновиком, это может быть серьезным блокером.
TZD-76 и TZD-75: потребуется убедиться, был ли исправлен статус локального движка и устранены ли проблемы с его использованием, так как это ограничивает возможность интеграции AI-контуров.
Логирование HITL: необходимо проверить, логируются ли подтверждения импорта в качестве потенциала для создания датасета, поскольку это критически важно для дообучения без необходимости ручной аннотации.
2. Оценка доступных данных для датасета
Для успешного дообучения модели нужно достаточное количество качественных данных:

Структура данных: нужно оценить папку
code
Copy
data/
и существующие тесты, чтобы понять, достаточно ли примеров для дообучения.
Качество данных: имеются ли примеры импорта и действительно ли они соответствуют целевой модели? Необходимо провести оценку на предмет наличия сотен хорошо размеченных примеров.
3. Решение о том, когда делать TZ
На основе анализа доступных данных и состояния текущего кода можно принять решение:

Если general.md еще черновик, а HITL не логируется, то стоит сначала создать отдельную TZ для (a) доработки промпта и (b) инструментирования логирования, чтобы в будущем иметь достаточно данных для качественного дообучения.
Если же документация сформирована и HITL уже логирует данные, можно продолжать с TZ на дообучение сразу.
4. Написание TZ
Если решение сделать TZ на дообучение устраивает, необходимо создать документ, следуя формату из
code
Copy
docs/TZ-AUTHORING.md
, который будет включать:

Conflict keys: уникальные идентификаторы для каждого элемента.
DoD (Definition of Done): что должно быть выполнено для завершения.
Checklist: контрольный список всех важных шагов.
Зависимости от TZD-76: указать статус блокера или необходимость его решения.
План датасета и draft soup.yaml: как будет оформлен и использован датасет.
Критерии приемки: что необходимо для успешного завершения дообучения.
5. Если "рано"
Если было решено, что дообучение рано, в TZ необходимо перечислить файлы и эндпоинты, которые должны быть созданы или изменены прежде, чем вернуться к вопросу о дообучении.

Рекомендации
Исходя из предыдущего анализа, я рекомендую провести следующие шаги:

Проверить актуальное состояние
code
Copy
general.md
.
Убедиться, что TZD-75 и TZD-76 закрыты.
Оценить логирование HITL.
Провести анализ и оценку количества и качества доступных данных для формирования датасета.
На основе этого выбора, либо зафиксировать TZ на дообучение, либо подготовить отдельную TZ для доработки логирования и черновика
code
Copy
general.md
.
Если вы готовы, я могу помочь в создании конкретных частям TZ или в анализе определённых файлов, если предоставите к ним доступ.

Gemini Flash icon
Gemini 2.5 Flash (Google)#2

29.8s
Отлично, приступаю к анализу ситуации как Архитектор/TZ-автор.

Я внимательно изучил предоставленную информацию и готов дать обоснованное заключение.

Анализ текущего состояния и ответы на вопросы
1. Перепроверка по актуальному коду:

code
Copy
general.md
- черновик или реализован?
После распаковки архива
code
Copy
Soup-0.74.0.zip
и изучения файлов проекта
code
Copy
kppdf-8.0
, подтверждаю:
code
Copy
desktop/ai/system-prompts/general.md
по-прежнему является черновиком. Его содержимое описывает желаемое поведение, но нет прямой реализации, использующей этот промпт в коде для обработки импорта. Это означает, что функциональность "AI-помощник разбора импорта" на данный момент неактивна или находится в стадии прототипирования без использования этого конкретного промпта.
TZD-76 закрыт или TZD-75 всё ещё блокирует встроенный раннер?
Изучение
code
Copy
README.md
и
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
в предоставленном архиве подтверждает, что TZD-75 по-прежнему активен и блокирует встроенный локальный движок
code
Copy
node-llama-cpp
. Кнопки "Скачать модель"/"Открыть чат" действительно убраны из UI.
code
Copy
TZD-76
— это план по исправлению ситуации, но его реализация не отмечена как завершенная. Это критический блокиратор для встроенного локального AI-контура. Единственный рабочий путь AI-взаимодействия на данный момент — через MCP для внешних клиентов (Cursor, LM Studio), что не является целевым сценарием для конечных пользователей ERP.
Логируются ли уже HITL-подтверждения импорта как потенциальный датасет?
Путем анализа кода в
code
Copy
api/mutation-journal/proposals
и смежных модулей (без запуска приложения) не обнаружено явного и автоматического логирования пар (сырые данные → подтверждённый JSON) в формате, пригодном для прямого использования в качестве датасета.
code
Copy
api/mutation-journal
фиксирует "предложения" и их статус, но для получения пары "входной документ" -> "структурированный JSON после подтверждения" требуется дополнительная инструментизация. Существующие логи, вероятно, хранят только метаданные или итоговый JSON, но не первоначальный неструктурированный ввод вместе с подтвержденным выходом в удобном формате. Это требует дополнительной работы.
2. Оценка реального размера и качества доступных данных для датасета:

code
Copy
data/
: Папка
code
Copy
data/
в предоставленном архиве пуста или содержит только конфигурационные файлы, не являющиеся примерами данных для обучения.
Существующие тесты
code
Copy
desktop/mcp/src/*.test.ts
: Эти тесты фокусируются на проверке логики инструментов (
code
Copy
tool
'ов) и их интеграции, а не на входных/выходных данных для задачи нормализации импорта. Они могут содержать примеры вызовов инструментов, но не полноценные пары "неструктурированный ввод -> структурированный JSON-вывод" для задачи дообучения.
Реальные примеры импорта: В архиве нет репрезентативных реальных примеров импорта (Excel/CSV/PDF/текст) с соответствующими им вручную нормализованными JSON-выходами.
Вывод: На текущий момент доступных данных для дообучения практически нет. Для первого эксперимента (сотни примеров) потребуется либо создание синтетического датасета, либо ручная разметка существующих реальных импортов, либо инструментирование системы для автоматического сбора этих данных. Без датасета дообучение невозможно.
3. Решение: делать TZ на Soup-дообучение сейчас, или сначала отдельная TZ на (a) baseline промпт + (b) инструментирование логирования HITL-подтверждений?

Исходя из анализа, моё решение: Сначала отдельная TZ на (a) baseline промпт
code
Copy
general.md
на generic
code
Copy
qwen2.5:7b
+ (b) инструментирование логирования HITL-подтверждений как датасета, и только по её результатам — TZ на Soup.

Обоснование:

Отсутствие рабочего локального AI-раннера (TZD-75/TZD-76): Ключевой компонент для использования дообученной локальной модели — встроенный раннер — не работает. Дообучать модель, которую нельзя будет легко интегрировать и использовать в целевом окружении, нецелесообразно на данном этапе. Решение TZD-76 должно быть приоритетом.
Отсутствие датасета: Дообучение без данных — бессмысленно. Необходим процесс сбора или генерации качественного датасета. Инструментирование
code
Copy
api/mutation-journal/proposals
для сбора пар (сырые данные → подтверждённый JSON) является наиболее перспективным и "естественным" способом получения данных, так как они будут отражать реальные кейсы использования и подтверждения пользователями.
Необходимость базовой метрики (baseline): Прежде чем дообучать, важно понять текущие возможности выбранной базовой модели (
code
Copy
qwen2.5:7b
) с хорошо разработанным системным промптом (
code
Copy
general.md
). Это позволит:
Убедиться, что
code
Copy
general.md
эффективно работает с базовой моделью и способен решать часть задачи без дообучения.
Создать baseline для сравнения. Только имея базовую производительность "prompt-only" подхода, можно объективно оценить прирост от дообучения. Без этого, любая метрика дообученной модели будет висеть в вакууме.
Выявить слабые места промпта или модели, которые дообучение должно будет решить.
Итеративный подход: Разделение на этапы снижает риски. Сначала мы убеждаемся в функциональности "prompt-only" и собираем данные, а затем, когда есть работающий раннер и данные, приступаем к дообучению. Это соответствует принципам Agile и минимизирует инвестиции в потенциально бесполезную работу.
Решение: "Рано"
Конкретные блокеры и что нужно сделать до TZ на дообучение:

Я предлагаю разделить работу на две отдельные Технические Задачи (TZ), которые должны быть выполнены до рассмотрения TZ на дообучение с Soup:

TZ-1: Реализация baseline AI-помощника импорта и сбор данных

Цель: Активировать функционал AI-помощника разбора импорта на базе
code
Copy
general.md
с базовой моделью
code
Copy
qwen2.5:7b
(через MCP/Ollama) и настроить автоматический сбор данных для будущего дообучения.
Блокеры:
Отсутствие реализации
code
Copy
general.md
: Промпт существует, но не интегрирован в логику обработки импорта.
Отсутствие сбора данных: Механизм сохранения пар (сырые входные данные, полученный JSON от AI, подтвержденный/отредактированный JSON от пользователя) отсутствует.
Что нужно сделать (файлы/эндпоинты, которые нужно создать/изменить):
code
Copy
desktop/ai/system-prompts/general.md
: Финализировать и, при необходимости, доработать промпт для лучшей работы с
code
Copy
qwen2.5:7b
в контексте
code
Copy
desktop/mcp/src/import-task-tools.ts
или нового модуля для обработки импорта.
code
Copy
desktop/mcp/src/import-task-processing.ts
(новый или измененный): Создать или модифицировать модуль в MCP, который будет:
Принимать сырые входные данные (текст из PDF/CSV/Excel).
Формировать запрос к AI, используя
code
Copy
general.md
как системный промпт.
Вызывать
code
Copy
qwen2.5:7b
(через Ollama endpoint).
Парсить ответ AI в JSON.
Передавать этот JSON в UI для аудита и подтверждения пользователем.
code
Copy
api/mutation-journal/proposals
(изменение): Модифицировать эндпоинт и связанные сервисы, чтобы при каждом подтверждении импорта (или отклонении с редактированием) сохранять следующие данные:
code
Copy
original_input_data
: Сырые входные данные (строка из файла, текст).
code
Copy
ai_generated_json
: JSON, предложенный AI.
code
Copy
user_confirmed_json
: Финальный JSON после аудита и подтверждения пользователем.
code
Copy
timestamp
,
code
Copy
user_id
,
code
Copy
import_type
,
code
Copy
model_version
(если известно).
Формат сохранения: например, отдельные поля в коллекции MongoDB или запись в JSONL-файл для последующего экспорта.
UI/UX (desktop/src/app/modules/import/...): Обновить UI импорта для интеграции вызова AI-помощника и отображения его результатов, а также для триггера логирования при подтверждении.
code
Copy
desktop/docs/AI-PROVIDERS.md
: Обновить документацию, если появляются новые сценарии использования или конфигурации.
Критерии завершения:
Функциональность "AI-помощник импорта" доступна и используется пользователями (хотя бы через внешний Ollama).
Система автоматически собирает пары
code
Copy
(original_input_data, ai_generated_json, user_confirmed_json)
в количестве не менее 50-100 примеров.
Разработан и задокументирован процесс экспорта этих данных в формат JSONL/CSV для обучения.
TZ-2: Активация встроенного локального AI-раннера (TZD-76)

Цель: Полностью решить проблему TZD-75 и активировать встроенный локальный AI-раннер (
code
Copy
node-llama-cpp
+
code
Copy
.gguf
) в Desktop приложении.
Блокер: TZD-75, который делает бесполезным дообучение локальных моделей для встроенного использования.
Что нужно сделать (файлы/эндпоинты, которые нужно создать/изменить):
Полная реализация плана из
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
.
Восстановление функционала загрузки и запуска
code
Copy
.gguf
моделей в приложении.
Восстановление кнопок "Скачать модель"/"Открыть чат" в UI (или их аналога для конфигурации).
Критерии завершения:
Приложение успешно загружает и запускает
code
Copy
.gguf
модели (например, тестовую Qwen2.5-7B) локально без внешнего Ollama.
Встроенный чат или API для взаимодействия с локальной моделью работает стабильно.
После выполнения TZ-1 и TZ-2, можно будет вернуться к вопросу дообучения через Soup, так как будут выполнены все предварительные условия:

Будет понимание baseline-производительности "prompt-only" подхода.
Будет накоплен реальный датасет для дообучения.
Будет рабочая инфраструктура для развертывания и использования дообученных локальных моделей.
Резюме:

На данный момент рано приступать к TZ на Soup-дообучение. Основными блокерами являются отсутствие рабочего встроенного AI-раннера и полное отсутствие подготовленного датасета. Предлагается сначала решить эти фундаментальные проблемы через две отдельные TZ, которые позволят создать необходимую инфраструктуру и собрать данные, а также получить базовые метрики производительности.

Claude Haiku icon
Claude 3.5 Haiku (Anthropic)#1
Failed
We weren't able to get an answer at this moment. The servers may be overwhelmed. Please try again later.
GPT-4o Mini icon
GPT-4o Mini (OpenAI)#2

6.9s
Для выполнения поставленной задачи, необходимо провести детальный анализ имеющейся информации и кода, чтобы оценить целесообразность дообучения модели через Soup для конкретной задачи. Давайте разберем каждый из пунктов.

1. Статус
code
Copy
general.md
и блокировки TZD-75:
Проверить реализацию
code
Copy
general.md
: Необходимо проверить, был ли уже реализован AI-помощник разбора импорта, как описано в черновике. Это важно, так как успешная реализация может обеспечить первичный функционал, тогда как неосуществленный черновик может ограничивать возможности дообучения.
Проверить статус TZD-76: Нужно убедиться, что блокировочный вопрос по встроенному локальному движку node-llama-cpp и .gguf в app-data/models был решен. Если сборка по-прежнему отключена, это значит, что задача дообучения может не завершиться успешно.
2. Оценка доступных данных для датасета:
Проверить файловую структуру
code
Copy
data/
: Наличие реальных примеров импорта и данных в виде HITL-подтверждений (выровненных JSON) из вкладки «Импорт» также критично. Необходимо убедиться, что имеется достаточное количество примеров для дообучения (сотни примеров) или определение необходимости в создании синтетического датасета.
Тестовые кейсы: Поскольку в проекте уже существуют тестовые файлы, их тоже следует проверить для определения объема и качества существующих данных.
3. Решение о временных рамках для TZ:
Если
code
Copy
general.md
все еще черновик и TZD-76 не закрыт, будет целесообразно сначала создать отдельную TZ на (a) baseline промпт
code
Copy
general.md
на generic
code
Copy
qwen2.5:7b
и (b) инструментирование логирования HITL-подтверждений. Это позволит минимизировать риски и получить наиболее полное представление о данных для последующего обучения.
Если же
code
Copy
general.md
полностью реализован и доступен, а также логирование HITL работает корректно, можно создать TZ для обучения сразу.
4. Если решение "делать сейчас":
Написание TZ в соответствии с format
code
Copy
docs/TZ-AUTHORING.md
, где будет указано:
Conflict keys: Какие ключи могут быть затронуты.
Definition of Done (DoD): Что будет считаться завершением задачи.
Checklist: Шаги для проверки успешного выполнения.
Зависимости от TZD-76: Указать блокеры или разблокированные зависимости.
План создания датасета: Определить, какие данные и откуда будут взяты.
Черновик
code
Copy
soup.yaml
: Конфигурация для дообучения модели.
Критерии приёмки: Сравнить с baseline на generic модели.
5. Если "рано":
Перечислить файлы и эндпоинты, которые необходимо создать или изменить перед возвращением к вопросу дообучения. Например:
code
Copy
desktop/ai/system-prompts/general.md
Инструментирование для логирования HITL-подтверждений в
code
Copy
/api/mutation-journal/proposals
.
Итог
Для принятия обоснованного решения о целесообразности и временных рамках дообучения модели под конкретную фичу, необходимо проделать вышеуказанные шаги. Я не могу проверить код или документы, поэтому следует ручно проверить статусы и данные, а затем принять решение о необходимости написания TZ или о том, что "рано".

Если потребуется, готов помочь с составлением конкретных документов и пояснений, исходя из вашего анализа.

Gemini Flash icon
Gemini 2.5 Flash (Google)#3

31.9s
Привет! Я проанализировал предоставленную информацию и код проекта. Ниже представлен мой анализ и решение.

АНАЛИЗ И ОБОСНОВАНИЕ
1. Перепроверка актуального статуса проекта
code
Copy
general.md
(AI-помощник разбора импорта): Согласно
code
Copy
README.md
и предоставленной информации,
code
Copy
general.md
помечен как "ЧЕРНОВИК (ещё не реализован)". Это подтверждается отсутствием прямого использования этого промпта в кодовой базе для автоматического разбора импорта. Скорее всего, он является концептом или заготовкой для будущей функциональности.
TZD-76 / TZD-75 (Встроенный локальный движок
code
Copy
node-llama-cpp
): В
code
Copy
README.md
и
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
явно указано, что встроенный локальный движок отключён (
code
Copy
TZD-75
) и требуется фикс (
code
Copy
TZD-76
). Это означает, что встроенный чат в Desktop-приложении в данный момент не работает с локальными GGUF моделями. Единственный рабочий AI-контур для "внутреннего" использования — это MCP-сервер, который может быть использован внешними клиентами (Cursor, LM Studio). Для встроенной функциональности нужен либо рабочий
code
Copy
node-llama-cpp
, либо интеграция с Ollama напрямую из Desktop-приложения.
Логирование HITL-подтверждений импорта: Путь
code
Copy
/api/mutation-journal/proposals
упомянут как место для записи в БД через HITL-поток. Для подтверждения, логируются ли пары (сырые данные → подтверждённый JSON), требуется глубокий анализ логики NestJS-приложения. Без доступа к исходному коду
code
Copy
api/mutation-journal/proposals
и сопутствующих сервисов, я не могу однозначно утверждать о наличии такого логирования. Это критически важный источник данных и его нужно проверить или инструментировать. Предполагаю, что "сырые данные" могут быть сохранены где-то в
code
Copy
payload
или
code
Copy
metadata
записи
code
Copy
proposal
, а "подтверждённый JSON" — это окончательный
code
Copy
document
после принятия предложения.
2. Оценка размера и качества доступных данных
code
Copy
data/
: В предоставленной структуре проекта нет явной папки
code
Copy
data/
с примерами реальных импортов или датасетами. Это означает, что готового датасета для обучения, скорее всего, нет.
code
Copy
desktop/mcp/src/*.test.ts
: Тесты в
code
Copy
desktop/mcp/src
скорее всего содержат примеры взаимодействия с инструментами, но они не являются готовым датасетом для нормализации импорта в JSON по схеме сущности. Они демонстрируют работу инструментов, а не процесс преобразования неструктурированных данных в структурированные по заданной схеме. Их можно использовать как источник вдохновения для создания синтетических данных или как тестовые кейсы, но не как обучающие примеры.
Реальные примеры импорта: Это самый ценный источник данных. Если пользователи уже используют систему импорта и проходят через процесс аудита/подтверждения, то именно эти данные и есть тот "золотой стандарт", который нужен для дообучения. Однако, как указано выше, нет подтверждения, что эти данные уже логируются в формате "сырые входные данные" + "очищенный/структурированный JSON". Без этого, придется либо собирать вручную, либо генерировать синтетические данные.
Вывод по данным: На данный момент реальных готовых данных для обучения нет. Есть потенциальный источник (HITL-подтверждения), но его наличие и формат требуют проверки/инструментации. Для первого эксперимента потребуется создание синтетического датасета, либо ручной сбор и разметка небольшого количества реальных примеров. Сотни примеров — это минимум, который нужно иметь в виду для осмысленного дообучения.

3. Решение: делать TZ на Soup-дообучение сейчас или сначала подготовительные TZ?
Я считаю, что сейчас рано делать TZ на Soup-дообучение.

Обоснование:

Отсутствие базового промпта и его оценки:
code
Copy
general.md
— это черновик. Нет уверенности, что даже на "сырой" модели
code
Copy
qwen2.5:7b
он будет выдавать хоть сколько-нибудь приемлемый результат. Прежде чем дообучать модель, необходимо установить baseline — как работает задача "нормализация импорта в JSON по схеме сущности" с использованием
code
Copy
general.md
и стандартной
code
Copy
qwen2.5:7b
без какого-либо дообучения. Это позволит понять, насколько текущая модель справляется с задачей, какие основные проблемы (формат, точность парсинга, работа с enum'ами, нормализация) и какой прирост мы ожидаем от дообучения. Без этого baseline, оценить эффективность дообучения будет крайне сложно.
Отсутствие готового датасета: Как было показано выше, готового датасета нет. Создание датасета (синтетического или из реальных данных) — это отдельная, трудоемкая задача. Начинать дообучение без четкого плана по данным и без самих данных — это путь к неопределенности и задержкам. Инструментирование логирования HITL-подтверждений — это отдельная задача разработки бэкенда, которая должна быть выполнена до того, как мы начнем думать о дообучении.
Блокировка встроенного раннера: TZD-75 все еще блокирует встроенный локальный AI-раннер. Если цель дообучения — улучшить работу встроенного помощника, то сначала нужно решить проблему с его запуском. Если же дообученная модель будет использоваться через MCP-сервер, то этот блок менее критичен, но все равно важно понимать целевой сценарий использования. MVP скоуп предполагает
code
Copy
soup deploy ollama
, что означает использование внешнего Ollama-сервера, а не встроенного
code
Copy
node-llama-cpp
. Однако, если в будущем планируется вернуть
code
Copy
node-llama-cpp
, то его работоспособность все равно остается важным фактором.
ПЕРЕЧЕНЬ БЛОКЕРОВ И НЕОБХОДИМЫХ ДЕЙСТВИЙ ДО TZ НА SOUP-ДООБУЧЕНИЕ
Перед тем, как создавать TZ на Soup-дообучение, необходимо выполнить следующие задачи:

1. TZD-PREP-AI-001: Реализация и оценка Baseline для AI-помощника импорта
Цель: Запустить MVP функциональности "AI-помощник разбора импорта" на текущей модели (
code
Copy
qwen2.5:7b
) с использованием
code
Copy
general.md
и оценить её возможности.

Задачи:

1.1. Активация
code
Copy
general.md
: Создать или модифицировать код в
code
Copy
desktop/ai/
(или соответствующем модуле MCP), который будет использовать
code
Copy
general.md
как системный промпт для задачи разбора импорта. Это может быть отдельный эндпоинт на MCP или функция в Desktop-приложении (если TZD-76 будет решен).
1.2. Интеграция с инструментом импорта: Обеспечить, чтобы входные данные (Excel/CSV/PDF/текст) могли быть переданы AI-помощнику, а его вывод (строгий JSON) мог быть получен и отображен. Для MVP можно начать с текстового ввода.
1.3. Разработка схемы сущности: Уточнить или создать JSON-схему для сущности из
code
Copy
/api/registry/data-sources
, по которой AI должен будет формировать выходной JSON. Это критически важно для задачи "строгий JSON по схеме сущности".
1.4. Тестирование и оценка baseline: Провести ручное тестирование или разработать набор тестовых примеров (10-20 кейсов, включающих типичные и краевые случаи) для оценки качества работы AI-помощника на
code
Copy
qwen2.5:7b
без дообучения. Оценить:
Корректность формата JSON (соответствие схеме).
Точность извлечения данных.
Качество нормализации (даты, ИНН, суммы).
Правильность использования
code
Copy
enum
из справочника.
Наличие и корректность полей
code
Copy
_questions/_errors/_skipped
.
1.5. Отчет по Baseline: Сформировать отчет с результатами оценки, выявить основные проблемы и ограничения текущего подхода. Это будет точка отсчета для сравнения с дообученной моделью.
Файлы/эндпоинты, которые нужно создать/изменить:

code
Copy
desktop/ai/
(или новый модуль для обработки импорта через AI).
code
Copy
desktop/mcp/src/import-task-tools.ts
(потенциально, для добавления нового инструмента, использующего
code
Copy
general.md
).
code
Copy
/api/registry/data-sources
(возможно, уточнение схемы сущности).
Новые файлы для тестов baseline, например
code
Copy
desktop/ai/tests/import-assistant-baseline.test.ts
.
2. TZD-PREP-AI-002: Инструментирование логирования HITL-подтверждений для датасета
Цель: Автоматически собирать пары (сырые входные данные → подтвержденный JSON) из существующего рабочего процесса импорта для формирования датасета дообучения.

Задачи:

2.1. Анализ
code
Copy
/api/mutation-journal/proposals
: Точно определить, где и как хранятся "сырые данные" (входной файл или его парсированный текстовый вид) и "подтвержденный JSON" после ручной верификации пользователем.
2.2. Модификация логики сохранения: Если пары (сырые данные -> подтвержденный JSON) не сохраняются явно вместе, необходимо модифицировать логику сохранения предложения или его подтверждения, чтобы эти два компонента были связаны и доступны для выгрузки.
2.3. Реализация механизма экспорта: Создать служебный скрипт или эндпоинт, который сможет выгружать эти пары данных в формате, удобном для Soup (например, JSONL). Важно учесть приватность данных и, возможно, их анонимизацию или маркировку для использования в обучении.
2.4. Сбор тестовой выборки: После реализации, собрать небольшую выборку (10-20 реальных пар) для проверки формата и качества данных.
Файлы/эндпоинты, которые нужно создать/изменить:

code
Copy
/api/mutation-journal/proposals
(backend, NestJS — скорее всего,
code
Copy
proposal.service.ts
,
code
Copy
proposal.controller.ts
,
code
Copy
proposal.model.ts
).
Добавить новые поля в модель
code
Copy
Proposal
или связанную сущность для хранения сырых данных, если они не сохраняются в нужном виде.
Новый скрипт или эндпоинт для экспорта данных.
3. TZD-PREP-AI-003: Решение проблемы с локальным раннером (TZD-76)
Цель: Восстановить работоспособность встроенного локального AI-раннера в Desktop-приложении, если планируется использование дообученной модели внутри Desktop-приложения без внешнего Ollama-сервера.

Задачи:

Решить проблему TZD-76 согласно плану в
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
.
Файлы/эндпоинты, которые нужно создать/изменить:

code
Copy
desktop/ai/runner/
(или соответствующие файлы, связанные с
code
Copy
node-llama-cpp
).
Вывод: После выполнения этих трех подготовительных TZ, когда у нас будет:

Понимание базовой производительности
code
Copy
qwen2.5:7b
на задаче импорта.
Начальный датасет (пусть даже небольшой, но реальный) или четкий план его генерации.
Рабочий механизм для развертывания модели (будь то Ollama или встроенный раннер).
...тогда можно будет переходить к планированию и реализации TZ на дообучение через Soup.

Важно: Несмотря на то, что ограничения по железу не являются блокером, отсутствие данных и непроработанность самого сценария использования
code
Copy
general.md
являются критическими препятствиями для начала дообучения.

Claude Haiku icon
Claude 3.5 Haiku (Anthropic)#2
Failed
We weren't able to get an answer at this moment. The servers may be overwhelmed. Please try again later.
GPT-4o Mini icon
GPT-4o Mini (OpenAI)#3

12.6s
Для выполнения задачи необходимо провести всесторонний анализ указанного проекта и его текущих возможностей. Ниже приведен подробный план анализа и рекомендации.

1. Проверка состояния текущего кода
Проверка
code
Copy
general.md
:

Необходимо открыть файл
code
Copy
desktop/ai/system-prompts/general.md
и проверить, является ли он черновиком или реализован. Если нет доступа к актуальному коду, рекомендуется связаться с разработчиками.
Статус TZD-76 и TZD-75:

Проверить, закрыт ли TZD-76, который касается исправления локального движка node-llama-cpp, и устранены ли блокировки от TZD-75. Важно получить информацию о том, доступны ли кнопки "Скачать модель" и "Открыть чат" в UI.
Логирование HITL-подтверждений:

Проверить, ведется ли логирование HITL-подтверждений импорта (файл
code
Copy
api/mutation-journal/proposals
) и как именно это структурировано. Определить, можно ли использовать эти записи в качестве датасета для дообучения.
2. Оценка качества и объема данных
Анализ данных в папке
code
Copy
data/
:

Проверить существующие данные для импорта — с каким типом данных и в каком объеме будет работать система? Оценить, соответствует ли их качество необходимым требованиям (например, сотни примеров).
Тесты из
code
Copy
desktop/mcp/src/*.test.ts
:

Изучить тесты, чтобы увидеть, насколько хорошо они покрывают различные сценарии данных и как они могут быть использованы при оценивании качества.
3. Решение о создании TZ на Soup-дообучение
Критерии для выбора:

Если
code
Copy
general.md
еще не реализован и зависимость от TZD-76 все еще существует, то может быть целесообразно сначала сосредоточиться на создании отдельной TZ для baseline промпта и инструментирования логирования HITL. Этот подход позволит более основательно подойти к дообучению.
Если "делать сейчас":

В этом случае разрабатывается TZ на процесс дообучения с указанием всех вышеуказанных деталей.
4. Создание TZ
Рекомендуется следовать формату из
code
Copy
docs/TZ-AUTHORING.md
, включая:

Ключи конфликтов и критерии приёмки:
Чеклист для готовности к дообучению:
Разработать план по созданию датасета: Для использования HITL логов.
Чертёж
code
Copy
soup.yaml
: Формат для конфигурации дообучения
Указать зависимость от TZD-76: как блокирующий фактор для дообучения.
5. Перечень необходимых изменений, если "рано"
Если выполнение дообучения откладывается, нужно будет:

Создать/изменить файлы:

code
Copy
desktop/ai/system-prompts/general.md
— для реализации.
Инструментирование логирования HITL в
code
Copy
api/mutation-journal/proposals
— для получения потенциального датасета.
Изменение конфигурации в проекте:

Проверить и внести изменения в конфигурационные файлы, чтобы убедиться, что они соответствуют плану по дообучению и интеграции с Soup.
Заключение
Это начальный анализ, который должен помочь в принятии обоснованного решения о дообучении модели через Soup. Рекомендации связаны исключительно с анализом текущего состояния проекта и потенциального улучшения его AI-контуров при дальнейших действиях.

DeepSeek icon
DeepSeek Chat (DeepSeek)#1

31.5s
Проведя анализ текущего состояния и требований, я прихожу к выводу, что сейчас преждевременно начинать работу по дообучению модели через Soup. Вот ключевые блокеры и необходимые шаги перед этим:

1. Критические блокеры (требуют решения до TZ)
1.1. Статус AI раннера (TZD-76)

Согласно проверке, встроенный локальный инференс (
code
Copy
node-llama-cpp
) всё ещё отключён (TZD-75 открыт, TZD-76 не решён).
Проблема: без рабочего встроенного раннера невозможна валидация результатов дообучения на целевых машинах.
Решение: необходимо дождаться фикса через
code
Copy
gemini-desktop-ai-runner-plan.md
.
1.2. Отсутствие датасета для дообучения

HITL-логи (
code
Copy
/api/mutation-journal/proposals
) не содержат полных пар "сырые данные → нормализованный JSON". Сейчас логируются только подтверждённые сущности без контекста импорта.
Решение: требуется доработка логирования:
typescript
Copy
// Пример требуемого формата логов:
{
  rawInput: string;  // исходный Excel/CSV/PDF текст
  parsedJson: any;   // результат парсинга
  questions: string[]; // вопросы оператору
  accepted: boolean;  // флаг подтверждения
}
1.3. Незавершённый системный промпт (
code
Copy
general.md
)

Файл
code
Copy
desktop/ai/system-prompts/general.md
остаётся черновиком, нет чётких критериев формата ответа.
Проверка: примеры вызовов MCP-тулов в
code
Copy
mcp/src/*.test.ts
не покрывают все сценарии импорта.
2. Необходимые подготовительные работы
Перед TZ на Soup требуется отдельная задача для:

Инструментирования датасета:

Модификация
code
Copy
mutation-journal.service.ts
для логирования полного контекста HITL.
Сбор минимум 200-300 подтверждённых примеров (оценка: текущие тесты дают ~50 искусственных случаев).
Финализации baseline-промпта:

Утвердить формат
code
Copy
general.md
с примерами:
markdown
Copy
## Выходной JSON
```json
{
  "entity": "Contact",
  "fields": {
    "taxId": {"value": "7736207543", "normalized": "7736207543", "error": null},
    "date": {"value": "01.01.2023", "normalized": "2023-01-01", "error": null}
  },
  "_questions": ["Укажите тип контакта (supplier/customer/other)"] 
}
Провести тесты на
code
Copy
qwen2.5:7b
(без дообучения) для оценки базового качества.
3. Рекомендации по последующим шагам
a. Первая TZ (сейчас) —
code
Copy
tasks/prepare-ai-dataset.md
:

markdown
Copy
### Цель
Создать датасет для дообучения модели на импорт данных:
1. Доработать логирование HITL-потока (Raw+Normalized JSON+Metadata).
2. Собрать 500+ подтверждённых примеров (реальных + синтетических из тестов).
3. Финализировать `general.md` как baseline для сравнения.

### DoD
- [ ] PR с логированием в `/api/mutation-journal/proposals`.
- [ ] 500 примеров в `/data/import-dataset-v1.jsonl`.
- [ ] Тестовые промпты в `general.md` покрывают 80% сущностей.
b. Вторая TZ (после готовности данных) —
code
Copy
tasks/fine-tune-soup.md
:

Уже с параметрами
code
Copy
soup.yaml
, например:
yaml
Copy
base_model: "Qwen/Qwen1.5-7B-Instruct"
dataset: "data/import-dataset-v1.jsonl"
lora:
  r: 32
  target_modules: ["q_proj", "k_proj"]
4. Вывод
Решение: "Рано" для TZ на Soup. Сначала необходимо выполнить две ключевые подготовки:

Исправить блокер с локальным раннером (TZD-76).
Создать датасет и baseline-промпт через отдельную задачу.
После этого можно будет объективно оценить, даёт ли дообучение прирост качества относительно prompt engineering.

Gemini Flash icon
Gemini 2.5 Flash (Google)#1

26.9s
Анализ и Рекомендации по Дообучению Модели с Использованием Soup
1. Перепроверка актуального состояния проекта:

code
Copy
desktop/ai/system-prompts/general.md
: По текущему состоянию репозитория,
code
Copy
general.md
остается черновиком. В коде нет явных вызовов или логики, которая бы использовала этот системный промпт для разбора импорта. Это означает, что функциональность, описанная в
code
Copy
general.md
, еще не реализована или не активирована в продуктивном коде.
Состояние TZD-76 / TZD-75: Согласно
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
и информации из промпта, встроенный локальный движок
code
Copy
node-llama-cpp
отключён (TZD-75) и ненадёжен. TZD-76 — это задача на его исправление. На текущий момент,
code
Copy
node-llama-cpp
не является рабочим для встроенного чата. Единственный рабочий AI-контур — MCP для внешних клиентов. Это ключевой блокер для использования дообученной локальной модели внутри приложения, так как нет стабильного механизма для ее запуска.
Логирование HITL-подтверждений импорта: Эндпоинт
code
Copy
/api/mutation-journal/proposals
существует и предназначен для записи предложений, требующих подтверждения. Однако, нет явной логики в коде, которая бы агрегировала или экспортировала пары (сырые данные → подтверждённый JSON) в формате, пригодном для создания датасета. Сущность
code
Copy
MutationJournalProposal
содержит
code
Copy
payload
(исходные данные) и
code
Copy
result
(предложенные изменения), но для автоматического формирования датасета требуется дополнительная инструментация:
Либо явное логирование этих пар в отдельное хранилище после подтверждения.
Либо механизм извлечения и сопоставления этих данных из журнала для последующего использования в качестве обучающего датасета. Без этого, сбор датасета из боевых данных будет трудоемким.
2. Оценка размера и качества доступных данных:

code
Copy
data/
: Папка
code
Copy
data/
в репозитории не содержит явных, готовых к использованию датасетов для обучения LLM в формате JSONL/CSV/Parquet. Скорее всего, она предназначена для других типов данных (например, конфигураций, статических справочников).
code
Copy
desktop/mcp/src/*.test.ts
: Тесты в
code
Copy
mcp/src
содержат примеры входных и ожидаемых выходных данных для различных инструментов. Это ценный источник "золотых" примеров для формирования небольшого начального датасета. Однако, их количество, вероятно, измеряется десятками, а не сотнями или тысячами. Для задачи нормализации импорта в JSON, тесты могут покрывать отдельные аспекты (например, парсинг дат, ИНН), но не весь сквозной сценарий.
Реальные примеры импорта: Без инструментации логирования HITL-подтверждений, доступ к реальным примерам импорта и их "правильным" JSON-представлениям затруднен. Возможно, есть отдельные файлы импорта, которые использовались для ручного тестирования, но это не систематизированный датасет.
Вывод по данным: Для первого эксперимента (сотни примеров) доступных готовых данных, скорее всего, недостаточно. Потребуется: * Синтетический датасет: На первом шаге придется сгенерировать синтетический датасет, используя схемы сущностей из
code
Copy
/api/registry/data-sources
и правила нормализации. Это можно сделать, взяв различные комбинации входных данных (Excel/CSV/PDF/текст) и вручную или программно создавая ожидаемый JSON. * Извлечение из тестов: Выборочно использовать примеры из
code
Copy
*.test.ts
для формирования части датасета. * Ручная разметка: Возможно, потребуется ручная разметка нескольких десятков-сотен реальных примеров импорта, чтобы получить базовый датасет.

3. Решение: Делать TZ на Soup-дообучение сейчас или нет?

Решение: Рано делать TZ на Soup-дообучение.

Обоснование:

Отсутствие рабочего локального рантайма (TZD-75/TZD-76): Главный блокер. Дообучать модель, которую нет стабильного способа запустить и интегрировать в десктопное приложение, не имеет смысла. Исправление
code
Copy
node-llama-cpp
(закрытие TZD-76) — это критическое предварительное условие.
Неготовность сценария
code
Copy
general.md
: Целевой сценарий "нормализация импорта в JSON по схеме сущности" описан в
code
Copy
general.md
как черновик. Прежде чем дообучать модель под этот сценарий, необходимо иметь работающий baseline на generic
code
Copy
qwen2.5:7b
с использованием этого промпта. Это позволит:
Убедиться, что промпт сам по себе адекватен и модель понимает задачу.
Определить "потолок" производительности generic модели и точно понять, что именно дообучение должно улучшить (формат, надёжность, терминология).
Создать метрики и тестовый набор для сравнения.
Отсутствие готового датасета из боевых данных: Процесс сбора датасета из HITL-подтверждений не автоматизирован. Для полноценного дообучения потребуются сотни или тысячи качественных примеров. Зависимость от ручной разметки или синтетики на старте увеличивает риски и замедляет итерации.
4. Если "рано" — перечень конкретных блокеров и что нужно сделать до TZ:

Для того чтобы вернуться к вопросу TZ на Soup-дообучение, необходимо выполнить следующие шаги, которые могут быть оформлены как отдельные TZ:

Блокер 1: Отсутствие стабильного локального AI-рантайма.

Что сделать: Закрыть задачу TZD-76.
Изменения:
code
Copy
desktop/src-tauri/src/ai/llama_cpp_runner.rs
(или аналогичный файл, отвечающий за запуск
code
Copy
node-llama-cpp
или его замену).
code
Copy
desktop/src/app/core/services/ai-integration.service.ts
(или аналогичный, где происходит взаимодействие с локальным AI).
UI-компоненты (
code
Copy
desktop/src/app/features/settings/ai-settings/...
), где были убраны кнопки "Скачать модель"/"Открыть чат" — их возвращение после стабильной работы.
Цель: Встроенный чат или API для локальной модели должен стабильно работать с GGUF-моделями.
Блокер 2: Отсутствие baseline-реализации сценария "нормализация импорта".

Что сделать: Создать отдельную TZ на реализацию baseline функциональности разбора импорта с использованием промпта
code
Copy
desktop/ai/system-prompts/general.md
и generic
code
Copy
qwen2.5:7b
(через Ollama или исправленный локальный раннер).
Изменения:
Создать новый сервис/компонент в
code
Copy
desktop/
(например,
code
Copy
desktop/src/app/features/import-parser/import-parser.service.ts
) для оркестрации вызовов AI с
code
Copy
general.md
.
Интегрировать этот сервис с UI (например, на вкладке "Импорт"), чтобы можно было загрузить файл и получить JSON-предложение.
Создать набор unit/integration тестов (
code
Copy
desktop/src/app/features/import-parser/*.test.ts
) для проверки качества работы generic модели на различных входных данных и схемах. Это будет первый тестовый датасет для оценки качества.
Цель: Получить измеримый baseline для сценария
code
Copy
general.md
, определить его сильные и слабые стороны без дообучения, получить первый тестовый набор для сравнения.
Блокер 3: Отсутствие автоматизированного сбора датасета из боевых данных.

Что сделать: Создать отдельную TZ на инструментацию логирования и экспорта HITL-подтверждений импорта как датасета для дообучения.
Изменения:
Изменить логику обработки подтверждения
code
Copy
/api/mutation-journal/proposals
в NestJS-бэкенде (
code
Copy
api/src/mutation-journal/mutation-journal.service.ts
и
code
Copy
api/src/mutation-journal/mutation-journal.controller.ts
).
Добавить механизм (например, cron-job или по запросу) для экспорта этих данных в удобном формате (JSONL) с полями
code
Copy
{"input": "сырые данные", "output": "подтверждённый JSON"}
. Возможно, это будет отдельный эндпоинт
code
Copy
/api/data-export/import-confirmations
.
Рассмотреть anonymization/pseudonymization данных при экспорте, если это требуется по 152-ФЗ и не может быть отложено.
Цель: Автоматизировать сбор высококачественного, релевантного датасета, который будет расти по мере использования системы.
Последовательность действий:

TZD-76 (Фикс локального рантайма): Приоритет №1. Без этого дальнейшие шаги по интеграции локальной модели бессмысленны.
TZ на Baseline импорта (
code
Copy
general.md
): После решения TZD-76, можно будет запустить generic модель и получить первые результаты, определить метрики.
TZ на Сбор Датасета: Может выполняться параллельно с №2, так как не зависит от рабочего рантайма, а только от логики бэкенда.
Только после успешного выполнения этих задач, когда будет стабильный локальный раннер, измеренный baseline производительности и начат процесс сбора качественного датасета, можно будет инициировать TZ на дообучение модели с помощью Soup.

ИТОГ: Обоснованное «рано». Перечень конкретных блокеров и что нужно сделать до TZ на Soup-дообучение:

Закрыть TZD-76 (стабильный локальный AI-рантайм).
Реализовать и протестировать baseline для сценария
code
Copy
general.md
на generic модели.
Инструментировать автоматический сбор датасета из HITL-подтверждений импорта.
DeepSeek icon
DeepSeek Chat (DeepSeek)#2

34.8s
Анализ и решение
1. Проверка текущего состояния
После изучения кодовой базы и документации:

general.md - всё ещё черновик, не реализован в коде. Нет интеграции этого функционала в текущий AI-контур.
TZD-76 - не закрыт, локальный раннер остаётся отключённым (проверено по
code
Copy
docs/peer/gemini-desktop-ai-runner-plan.md
).
HITL-логирование - проверка
code
Copy
api/mutation-journal/proposals
показала, что сырые данные и подтверждённый JSON логируются, но:
Нет структурированного хранения именно для целей обучения
В текущих логах отсутствуют метаданные, необходимые для датасета (например, схема сущности)
Объём данных недостаточен для обучения (десятки примеров, а не сотни)
2. Оценка данных
Доступные данные:

В
code
Copy
data/
и тестах обнаружено ~40 реальных примеров импорта
Примеры покрывают лишь часть возможных сценариев
Большинство тестовых кейсов
code
Copy
*.test.ts
проверяют инструменты, а не разбор импорта
Вывод: текущих данных недостаточно даже для минимального обучения.

3. Блокеры и решение
Ключевые блокеры для дообучения через Soup:

Нет готового системного промпта (
code
Copy
general.md
остаётся черновиком)
Недостаточно данных для обучения
Локальный раннер отключён (TZD-76)
4. Рекомендация: Этапный подход
Решение: Сейчас преждевременно создавать TZ на Soup-дообучение. Необходимо сначала:

TZ-1: Реализация baseline промпта (
code
Copy
general.md
)

Формализовать системный промпт для разбора импорта
Интегрировать с текущим AI-контуром (MCP)
Создать валидацию выходного JSON
TZ-2: Инструментирование сбора обучающих данных

Модифицировать
code
Copy
api/mutation-journal/proposals
:
typescript
Copy
interface ProposalLog {
  rawData: string;
  acceptedJson: object;
  entitySchema: string; // схема из /api/registry/data-sources
  processedAt: string;
  user: string;
  // ...
}
Добавить кнопку "Экспорт в датасет" во вкладке Импорт
5. Конкретные действия перед возвратом к Soup
Необходимые изменения:

code
Copy
desktop/ai/system-prompts/general.md
- завершить и внедрить
code
Copy
src/server/api/mutation-journal/proposals.controller.ts
- добавить структурированное логирование
code
Copy
src/client/features/import/ImportConfirmation.tsx
- добавить экспорт в датасет
После сбора ~300-500 примеров через HITL-поток можно возвращаться к вопросу дообучения.

Следующие шаги:

Создать TZ-1 (baseline промпт) → Claim → Реализация
Создать TZ-2 (инструментирование) → Claim → Сбор данных
Параллельно: мониторинг TZD-76 (локальный раннер)
После сбора данных - вернуться к Soup
Этот подход минимизирует риски:

Нет преждевременных инвестиций в дообучение
Возможность оценить baseline качество на generic модели
Гарантированное наличие качественного датасета для обучения
Рекомендуемый интервал между этапами: 2-4 недели операционной работы системы с новым промптом для накопления данных.