═══════════════════════════════════════════════════════════════
TZ-18: Production Hardening
═══════════════════════════════════════════════════════════════

РОЛЬ АГЕНТА: Backend Developer

ЗАВИСИМОСТИ: TZ-02, TZ-04

LAYER: 4

CONFLICT KEYS: backend/src/main.ts; backend/src/app.module.ts; backend/src/modules/rate-limit/**; backend/src/common/guards/throttler-behind-auth.guard.ts

═══════════════════════════════════════════════════════════════
ИСХОДНОЕ СОСТОЯНИЕ
═══════════════════════════════════════════════════════════════

1. TZ-02..TZ-17 завершены: backend работает, все модули реализованы, тесты зелёные.
2. TZ-04 уже создал User/Role/Permissions + JWT auth, но **не реализовал RateLimitEntry** (отложен в TZ-18).
3. Helmet + CORS + global ValidationPipe уже подключены (из TZ-02), но нужно настроить CORS whitelist через env.
4. Compression не подключён.

═══════════════════════════════════════════════════════════════
ЧТО ДЕЛАТЬ
═══════════════════════════════════════════════════════════════

ШАГ 1: RateLimitEntry (CRUD + интеграция)

```typescript
{ key: string; count: number; expiresAt: Date }  // TTL index
```

- `GET /rate-limits` — список (admin).
- `DELETE /rate-limits/:key` — очистить лимит.
- TTL index на `expiresAt` — MongoDB автоматически удаляет через 24ч.

ШАГ 2: Подключить `@nestjs/throttler` (rate limiting)

- `pnpm add @nestjs/throttler`.
- `app.module.ts`: `ThrottlerModule.forRoot([{name: 'short', ttl: 1000, limit: 10}, {name: 'long', ttl: 60000, limit: 100}])`.
- Custom `ThrottlerBehindAuthGuard` — пропускать rate limit если есть валидный JWT (но всё равно считать для IP).
- Применить глобально в `main.ts`: `app.useGlobalGuards(new ThrottlerBehindAuthGuard())`.

ШАГ 3: CORS whitelist через env

- В `main.ts`: `app.enableCors({origin: process.env.CORS_ORIGINS?.split(',') || ['http://localhost:4200', 'http://localhost:3000'], credentials: true})`.
- `CORS_ORIGINS` в `.env.example`: `http://localhost:4200,https://app.kppdf.ru`.

ШАГ 4: Compression

- `pnpm add compression`.
- `main.ts`: `app.use(compression())` (после helmet).

ШАГ 5: Helmet hardening

- `main.ts`: `app.use(helmet({contentSecurityPolicy: {directives: {defaultSrc: ["'self'"], scriptSrc: ["'self'"], styleSrc: ["'self'", "'unsafe-inline'"]}}, hsts: {maxAge: 31536000, includeSubDomains: true, preload: true}}))`.

ШАГ 6: Graceful shutdown

- `main.ts`:
  ```typescript
  app.enableShutdownHooks();
  // На SIGTERM:
  // 1. Stop accepting new connections
  // 2. Wait for in-flight requests (max 30s)
  // 3. Close MongoDB connection
  // 4. Exit
  ```
- `app.close()` через `app.getHttpServer().close()`.

ШАГ 7: Health check расширенный

- `GET /health` — добавить проверки: MongoDB ping, disk space, memory usage.
- `GET /health/ready` — readiness probe (готов ли принимать трафик).
- `GET /health/live` — liveness probe (жив ли процесс).

═══════════════════════════════════════════════════════════════
ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ
═══════════════════════════════════════════════════════════════

СОЗДАТЬ:
- `backend/src/modules/rate-limit/rate-limit.schema.ts, rate-limit.service.ts, rate-limit.controller.ts, rate-limit.module.ts`
- `backend/src/common/guards/throttler-behind-auth.guard.ts`

ОБНОВИТЬ:
- `backend/src/main.ts` (CORS, helmet, compression, throttler, graceful shutdown, расширенный health)
- `backend/src/app.module.ts` (ThrottlerModule, RateLimitModule)
- `backend/.env.example` (добавить CORS_ORIGINS, THROTTLE_TTL, THROTTLE_LIMIT)
- `backend/package.json` (добавить `@nestjs/throttler`, `compression`)

НЕ ТРОГАТЬ:
- `src/modules/auth/`, `src/modules/user/`, и т.п. (кроме main.ts/app.module.ts)

═══════════════════════════════════════════════════════════════
КРИТЕРИИ ПРИЁМКИ
═══════════════════════════════════════════════════════════════

1. `pnpm run build` без ошибок.
2. 100 быстрых запросов на `/health` → первые 100 проходят, 101-й → 429 (rate limit).
3. `CORS_ORIGINS=https://evil.com` в env → `curl -H "Origin: https://evil.com" http://localhost:3000/api/...` → 403 (CORS блокирует).
4. `curl -H "Accept-Encoding: gzip" http://localhost:3000/api/...` → ответ с `Content-Encoding: gzip`.
5. `GET /health` возвращает `{status: "ok", mongo: "up", uptime: 123, ...}`.
6. `GET /health/ready` и `/health/live` работают.
7. `docker compose stop backend` — graceful shutdown: логируется "Closing MongoDB connection...", процесс exit 0.
8. `GET /rate-limits` (admin) → пусто (rate limit counters очищаются).
9. **ГЛАВНОЕ:** файл `tasks/TZ-18.md` УДАЛЁН из `tasks/`.

═══════════════════════════════════════════════════════════════
TZF-00: ФИНАЛИЗАЦИЯ (ОБЯЗАТЕЛЬНО)
═══════════════════════════════════════════════════════════════

После прохождения всех критериев приёмки:

1. Записать в `progress.md` (по формату TZF-00 §3): дата, TZ-18, краткое описание.
2. Обновить `ARCHITECTURE.md` (по TZF-00 §4): см. Production hardening (rate limiting, helmet, CORS whitelist, graceful shutdown) ниже.
3. Скопировать `tasks/TZ-18.md` в `OrchestratorKit/_archive/<YYYY-MM>/TZ-18.done.txt` с маркером `ARCHIVE_MARKER`.
4. Обновить `OrchestratorKit/STATUS.md`: TZ-18 → ✅ DONE.
5. Запустить `bash OrchestratorKit/verify-status.sh` — должен быть PASS.
6. Создать `OrchestratorKit/.mimocode/locks/TZ-18-<slug>.lock`.
7. **Удалить `tasks/TZ-18.md`** (финальный сигнал Buffy что можно генерировать следующую TZ).
