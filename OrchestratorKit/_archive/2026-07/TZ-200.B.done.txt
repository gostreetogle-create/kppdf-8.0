TZ-200.B\n: ✓ Layer 0 verify-only DONE (orphan-каталоги не существуют per DIAGNOSTIC OVERRIDE 2026-07-25)

# TZ-200.B — RppEntry ⇄ Rpp canonical verification (split из TZ-200 per code-reviewer NEED-FIX #3)

**Дата:** 2026-07-25
**Тип:** Data-model consolidation (split из original TZ-200 per code-reviewer verdict)
**Priority:** 🟠 HIGH
**Status:** ⏳ READY
**Layer:** 4 (Backend — schemas + new join-entity)
**Agent Role:** Backend data-model engineer
**Estimated complexity:** M (1-3 days)

## 1. Контекст

Per code-reviewer verdict (NEED-FIX #3), split TZ-200 into TZ-200.B (Rpp canonical verification — Layer 0 doc-only) + TZ-200.C (WarehouseAccess M2M new entity creation — Layer 4 code work). This file now covers **Rpp verification only**.

`docs/data-model-audit.md` §1.1 #3 identifies `Rpp` ⇄ `RppEntry` as conceptual duplicate, but actual code shows only `rpp/` module exists.

## 2. Проблема

Audit references obsolete `RppEntry` schema not present in actual code. Documentation cross-references outdated terminology. Risk of future devs searching for `RppEntry` and finding misleading audit comments.

## 3. Файлы

### Files to verify (read-only)
- `backend/src/modules/rpp/rpp.schema.ts`
- `backend/src/modules/rpp/rpp.service.ts`
- `backend/src/modules/rpp/registry-number` (verify-canonical naming)

### Files to update (audit-doc only)
- `docs/data-model-audit.md` (§1.1 #3 mark DONE)
- `docs/data-model.md` (cross-ref cleanup)

### Files NOT to touch
- `backend/src/modules/warehouse/` (canonical, untouched; TZ-200.C covers new WarehouseAccess entity separately)
- `backend/src/modules/rpp/rpp-entry/` (был potential split per audit, но нет в code+)

## 4. Action Steps

1. Verify-canonical: `grep -rn "RppEntry" backend/src/` returns 0 (except audit-doc references).
2. Verify `rpp.schema.ts` JSDoc clearly states canonical naming vs legacy aliases.
3. Update `docs/data-model-audit.md`: §1.1 #3 status → **DONE (was conceptual duplicate; resolved at file-system level — only `rpp/` exists)**.
4. Update `docs/data-model.md`: remove any mentions of "RppEntry" schema module.

## 5. Acceptance Criteria

1. `grep -rn "RppEntry" backend/src/` returns 0 hits.
2. `docs/data-model-audit.md` §1.1 #3 row shows **DONE 2026-07-25 (TZ-200.B)**.
3. `pnpm exec tsc --noEmit` exit 0 в backend (changes are docs-only, sanity check).

## 6. Cross-references

- **Split из:** TZ-200 (initial draft) per first code-reviewer verdict, then **further split** per second code-reviewer (NEED-FIX #3) into this TZ-200.B (Rpp) + TZ-200.C (WarehouseAccess).
- **Упомянуто в:** `docs/data-model-audit.md` §1.1 #3 + §8.1 Priority 1 #1.8.

## 7. Out of Scope

- WarehouseAccess new schema creation (TZ-200.C — separate Layer 4 TZ).
- WorkOrderOperation (TZ-200.D candidate if needed in future).
- New entities (ProductPricing, etc — separately batched).

## 8. Finalization

TZF-00 standard flow. Lock: `.mimocode/locks/TZ-200.B-rpp-canonical-cleanup.lock`.

## 7. Out of Scope

- WorkerWorkType, ProductModuleAssignment, TaskDependency, CertificateProduct — separate TZ-204 batch.
- Warehouse.zoneNames refactor — separate.

## 8. Finalization

TZF-00 standard flow. Lock: `.mimocode/locks/TZ-200.B-rpp-canonical-cleanup.lock`.

═══════════════════════════════════════════════════════════════
ARCHIVE_MARKER
═══════════════════════════════════════════════════════════════
outcome: DONE
closed_at: 2026-07-25
closed_by: 
next_steps: —
