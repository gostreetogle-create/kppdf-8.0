TZ-82 — Browser-use smoke test · PENDING (INDEPENDENT)
═══════════════════════════════════════════════════════════════════
Date: 2026-07-05 · Status: 🕒 PENDING · Independent of TZ-80 (REJECTED).

Decision
─────────────────────────────────────────────────────────────────
Originally TZ-82 was a smoke-test runner that depended on TZ-80 (SSR
preview server on :4000). After TZ-80 was REJECTED, TZ-82 was
re-pointed to the existing dev server (ng serve :4200) — no SSR
required, the angular router + lazy routes already work in CSR mode.

Pending spec (to be executed as TZ-82b)
─────────────────────────────────────────────────────────────────
Stack: Playwright + Lighthouse + Codebuff browser-use agent fallback.
Runs against the existing dev server on :4200 (no separate node process).

Coverage:
  - 6 lazy routes: /overview /foundations /basics /forms /overlays
    /navigation + maybe /playground/{theme,code}
  - 2 viewports: 1280×800 (desktop) + 375×812 (mobile).
  - Per-route assertions:
      · no console errors
      · page-header renders (eyebrow + title + subtitle)
      · first pi-section visible
      · a11y: rerun audit:a11y from TZ-79 (now an independent check)
      · screenshot for visual regression baseline
  - Lighthouse audit: a11y ≥95, performance ≥80, best-practices ≥90,
    SEO ≥80 (no SSR, so SEO is best-effort — meta tags + robots.txt matter).
  - Drop regression screenshots in /tmp/smoke/<route>-<viewport>.png.

CI integration:
  - Add `pnpm run smoke` script in frontend/package.json that boots
    ng serve (or hits a running one) + runs Playwright + exits 0/1.
  - Optional: integrate TZ-79 audit:a11y as a pre-step in the same
    Playwright spec (single Chromium launch covers both).

Lock file
─────────────────────────────────────────────────────────────────
OrchestratorKit/.mimocode/locks/TZ-82-smoke-test.lock retained as
placeholder (still a planned TZ, no code changes yet).

Dependencies
─────────────────────────────────────────────────────────────────
· TZ-67 (KitLayoutComponent) — DONE
· TZ-69..74 (6 lazy pages) — DONE
· TZ-79 (axe-core audit) — DONE
· TZ-81 (README + docs) — DONE
  DOI: still independent of TZ-80 (REJECTED) and TZ-78 (highlight.js).
