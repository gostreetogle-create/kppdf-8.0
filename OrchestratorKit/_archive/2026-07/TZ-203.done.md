# TZ-203 — DocType ⇄ DocTypeDef canonical unification (added per code-reviewer verdict)

**Дата:** 2026-07-25
**Тип:** Data-model consolidation (new TZ covering audit §1.1 #1 missed in TZ-199..202 batch)
**Priority:** 🟡 MEDIUM
**Status:** ⏳ READY
**Layer:** 4 (Backend)
**Agent Role:** Backend data-model engineer
**Estimated complexity:** S (<1 day)

## 1. Контекст

Code-reviewer verdict on **TZ-199..202 batch** identified that audit `§1.1 #1` (`DocType` ⇄ `DocTypeDef`) was not covered by my 4-TZ batch. Per audit:
- `DocType` has `id, timestamps`
- `DocTypeDef` lacks `id/timestamps` (legacy, incomplete)
- Канон: `DocType` (merge DocTypeDef fields)

## 2. Проблема

Audit identified `DocTypeDef` as legacy/duplicate but no TZ currently covers migration. Audit references it in Open Questions §4.

Diagnostic (2026-07-25):
- `backend/src/modules/doc-type/` likely contains both modules.

## 3. Файлы

### Files to verify / modify
- `backend/src/modules/doc-type/` (verify both schemas exist or only `DocType`)
- If `DocTypeDef` exists as separate module → migrate data + delete module.
- If absent → audit §1.1 #1 is DONE (just doc-mark).

### Files NOT to modify
- `backend/src/modules/doc-type/doc-type.schema.ts` (canonical — keep)
- `backend/src/modules/document-template/` (only references DocType FK)

## 4. Action Steps

1. Verify: `ls backend/src/modules/doc-type/`. Confirm single `DocType` schema.
2. Grep: `grep -rn "DocTypeDef\|doc-type-def" backend/src/`. If absent → DONE.
3. If present (separate module):
   - Migration: `db.doctypedefs.find().forEach(...)` → upsert `db.doctypes` + add `id, timestamps`.
   - Drop `doctypedefs` collection.
4. Update `docs/data-model-audit.md` §1.1 #1 → **DONE**.

## 5. Acceptance Criteria

1. Single schema `DocType` exists. Single collection `doctypes`.
2. `grep -rn "DocTypeDef" backend/src/` returns 0 hits.
3. `docs/data-model-audit.md` §1.1 #1 status → **DONE 2026-07-25 (TZ-203)**.
4. `pnpm exec tsc --noEmit` exit 0.

## 6. Cross-references

- **Добавлен per:** code-reviewer verdict on TZ-199..202 batch.
- **Упомянуто в:** `docs/data-model-audit.md` §1.1 #1 + §8.1 Priority 1 #1.11 + §7.1 (Entities для удаления).

## 7. Out of Scope

- DocType enum extensions (separate concern).
- `DocumentTemplate` schema refactor — TZ-199's territory.

## 8. Finalization

TZF-00 standard flow. Lock: `.mimocode/locks/TZ-203-doctype-unify.lock`.
