# TZ-201 — Role/Roles + Client/Counterparty + DocType/DocTypeDef unification

**Дата:** 2026-07-25
**Тип:** Data-model consolidation (successor-TZ для `docs/data-model-audit.md` Priority 1 #5 + #7 + #10)
**Priority:** 🟠 HIGH
**Status:** ⏳ READY
**Layer:** 0 (Doc-only) + 4 (Backend — minimal verify)
**Agent Role:** Backend data-model engineer (verification-only)
**Estimated complexity:** M (1-3 days; Layer-0 doc-only по основной части — major verification)

## 1. Контекст

`docs/data-model-audit.md` §1.1 identifies:
- **#5**: `IncomingInvoice` ⇄ `Invoice` — rename canonical to `Invoice` (existing coll name: `invoices`).
- **#6**: `Role` ⇄ `Roles` — `Role` canonical, `status` field dead (use `isActive`).
- **#7**: `Employees` ⇄ `Worker` — `Worker` canonical, split `fullName` → `lastName`+`firstName`.
- **#10**: `MaterialCategory` + `ProductCategory` → `Category` (universal).

Per diagnostic (2026-07-25):
- `backend/src/modules/invoice/` exists with single schema. No separate `incoming-invoice/`. **Canonical `Invoice` is already in place.**
- `backend/src/modules/role/role.schema.ts` exists. No separate `roles/`. **Canonical `Role` is in place.**
- `backend/src/modules/worker/worker.schema.ts` exists. No separate `employees/`. **Canonical `Worker` is in place.**
- `backend/src/modules/category/category.schema.ts` exists. Materials reference `categoryId`, Products reference `categoryId` (both via FK). **Universal category уже partially consolidated.**

## 2. Проблема

Audit baseline указывает conceptual duplicates, но **most are already resolved at code-level**. Реальная работа:
- Audit-doc §1.1 #5/#6/#7/#10 rows updated to **DONE** (mark baseline).
- Verify no stray `status: string` fields in schemas (FSM — separate `TZ-203`-extension).
- Verify NIT `lastName+firstName` properly split в Worker rename-ready state.
- Document **canonical naming convention**: всё что read via `Role`, `Worker`, `Invoice` (singular) should match Mongoose collection names.

## 3. Файлы

### Files to modify
- `backend/src/modules/invoice/invoice.schema.ts` (collection name verify `invoices`)
- `backend/src/modules/role/role.schema.ts` (mark `isActive` field as canonical)
- `backend/src/modules/worker/worker.schema.ts` (verify `lastName`/`firstName` fields exist + add isActive docs)
- `backend/src/modules/category/category.schema.ts` (verify Consumer: Material/Product use `categoryId` FK)
- `docs/data-model-audit.md` (mark §1.1 #5/#6/#7/#10 как DONE)
- `docs/data-model.md` (cross-reference cleanup)

### Files NOT to modify
- `backend/src/modules/invoice/`
- `backend/src/modules/role/`
- `backend/src/modules/worker/`
- `backend/src/modules/category/`

## 4. Action Steps

1. Grep verify: `grep -rn "\"status\"\|\"Employees\"\|\"fulLName\"\|MaterialCategory\|ProductCategory" backend/src/` returns 0 hits.
2. Update `docs/data-model-audit.md`: §1.1 #5/#6/#7/#10 marked as **DONE (was conceptual naming duplicate; resolved at schema level)**.
3. Update `docs/data-model.md` to clarify "Worker (canonical) replaces Employees; see audit §1.1 #7".
4. Add cross-reference comment в `worker.schema.ts` indicating `lastName` + `firstName` come from split (legacy был `Employees.fullName`).
5. Add cross-reference comment в `category.schema.ts` indicating this is universal `Category` (replaces MaterialCategory/ProductCategory).
6. `pnpm exec tsc --noEmit` exit 0.

## 5. Acceptance Criteria

1. `grep -rn "Employees\|fulLName\|MaterialCategory\|ProductCategory" backend/src/` returns 0 hits (only legacy audit-doc references).
2. `docs/data-model-audit.md` §1.1 #5 + #6 + #7 + #10 marked **DONE**.
3. `grep -rn "status.*string" backend/src/modules/role/role.schema.ts` returns 0 hits (FSM-clean).
4. `pnpm exec tsc --noEmit` exit 0 в backend.
5. Existing auth-test + worker-test e2e suites PASS.

## 6. Cross-references

- **Расширяет:** TZ-200 pattern (mark-DONE в audit doc).
- **Блокирует:** НЕТ.
- **Зависит от:** НЕТ — fully independent.
- **Упомянуто в:** `docs/data-model-audit.md` §1.1 #5/#6/#7/#10 + §7.2 (Entities для переименования) + §8.1 Priority 1.

## 7. Out of Scope

- FSM unification to `statusId: ObjectId` (separate `TZ-203` for full coverage, this TZ is light cleanup).
- WorkerWorkType/ProductModuleAssignment/TaskDependency (audit §2.1 — separate TZ-204 — M2M с атрибутами).
- BankDetail (+) LegalEntity (separate TZ-205, larger scope).

## 8. Finalization

TZF-00 standard flow. Lock: `.mimocode/locks/TZ-201-role-worker-category-canonical.lock`.
