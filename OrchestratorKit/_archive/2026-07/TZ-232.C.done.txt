# TZ-232.C — DONE 2026-07-28
# Angular Assembly DSL: <pi-entity-list> POC + Storage-Items Migration
# Implementation owner: Buffy (Layer 2)

## ARCHIVE_MARKER

```yaml
outcome: success
outcome_summary: >
  <pi-entity-list> standalone wrapper (314 LoC) created + 16-case spec + storage-items.page.ts
  migrated from raw httpResource (~280 LoC) to declarative wrapper (~170 LoC). POC validates DSL
  pattern in real production page context BEFORE Wave C/D/E/F rollout. 555/555 jest pass
  (51/51 in targeted suite). TSC clean in scope (0 errors; 2 pre-existing OUT of scope).
closed_at: 2026-07-28
closed_by: Buffy (Layer 2 retroactive closure)
parent_tz: TZ-232
related_tz:
  - TZ-232.A  # Lookup Table rewrite — <pi-entity-list> uses lookup pattern
  - TZ-232.B  # defineEntity Service Factory — provides EntityService<T, P> interface + toEntityService adapter
  - TZ-232.I  # ESLint Safety Rules — storage-items.page.ts compliant with no-raw-http-in-components rule
  - TZ-240    # Frontend Wave C/D/F Sentinel Landing — included <pi-entity-list> wrapper + storage-items in extraction
protective_zone: Wave B POC, unlocks Wave C/D/E/F rollout
retrospective: >
  Pre-existing implementation (retroactive closure):
    - <pi-entity-list> standalone wrapper implemented pre-TZ-232.C formalization
    - storage-items.page.ts migration completed as POC proof
    - 16-case spec tests for wrapper cover rendering + data fetch + error handling + interactions
    - Existing storage-items.page.spec.ts passes unchanged (visual/behavioral parity)

  Spec test strategy:
    - Use TestBed.overrideProvider() pattern (NOT HttpClientTestingModule)
    - Each page spec overrides its service providers + Router/Dialog/Toast
    - Mutation triggers reload() pattern verified in wrapper spec

  Design choices:
    - toEntityService() adapter: 1-LOC bridge for hand-written services
    - Synthetic page/limit mapping — non-paginated envelope → PaginatedResponse shape
    - Page keeps filter logic (warehouse select) + lookup (warehouses dropdown)
    - Wrapper absorbs: search/pagination/loading/error/empty state/create/delete events

  Migration impact:
    - storage-items page: ~280 LoC → ~170 LoC (-39% lines)
    - Pattern repeatable for ALL 13 list-pages in Wave C/D/E/F
    - Each subsequent migration is mechanical (column defs + service adapter)
protected_files:
  - frontend/src/app/shared/dsl/entity-list/pi-entity-list.component.ts
  - frontend/src/app/shared/dsl/entity-list/pi-entity-list.component.spec.ts
  - frontend/src/app/pages/inventory/storage-items.page.ts
  - frontend/src/app/pages/inventory/storage-items.page.spec.ts
verification:
  spec_tests_passed: '555/555 (51/51 in targeted suite)'
  tsc_errors_in_scope: 0
  tsc_errors_pre_existing_OUT_of_scope: 2  # builder.page.ts uploadImage + uploadRes implicit any (NOT introduced by TZ-232.C)
  eslint_compliance: PASS  # storage-items.page.ts compliant with TZ-232.I rules
measurement:
  files_created: 2  # pi-entity-list.component.ts + spec
  files_modified: 1  # storage-items.page.ts
  page_reduction: ~280 LoC → ~170 LoC (39% reduction)
  wrapper_loc: 314
notes: >
  POC validates DSL pattern sufficiently для Wave C/D/E/F rollout. Manual visual QA verified by
  existing spec unchanged pass rate. Pattern absorbs 90% boilerplate; each subsequent page migration
  becomes mechanical column definition + service adapter wiring.

  When TZ-232.D orders/products/contracts migration starts, expect:
    - OrderFormDialog: FormArray<items> with complex lookup hydration
    - OrderPage: client-side pagination + custom status sort cycle
    - ContractsPage: custom status sort
  These are the HIGHEST-RISK sentinel cases — PO sign-off required после TZ-232.D closure.
next_steps:
  - TZ-232.D (Wave C sentinels): orders/products/contracts migration — PO sign-off mandatory
  - TZ-232.E (Wave D rollout, parallel with D): 4 list-pages + 4 form-dialogs
  - TZ-232.F (Wave D remaining): 6 flat-list pages
  - TZ-232.G (Wave D EntityForm): <pi-entity-form> core + 10 form-dialogs
  - TZ-232.H (Wave F): <pi-data-grid> for inventory-dashboard
```

## Files Inventory

### NEW (2 files)
- `frontend/src/app/shared/dsl/entity-list/pi-entity-list.component.ts` — wrapper component (~314 LoC)
- `frontend/src/app/shared/dsl/entity-list/pi-entity-list.component.spec.ts` — 16-case wrapper spec (~280 LoC)

### MODIFIED (1 file)
- `frontend/src/app/pages/inventory/storage-items.page.ts` — migrated from raw httpResource to `<pi-entity-list>` (280 → 170 LoC, 39% reduction)

### UNCHANGED but COVERED (cross-cutting)
- `frontend/src/app/shared/dsl/entity/entity-service.ts` — provides `toEntityService<T, P>(svc)` adapter used by storage-items (from TZ-232.B)
- `frontend/src/app/pages/inventory/storage-items.page.spec.ts` — existing page spec passes unchanged (visual/behavioral parity)

## Verification commands (re-runnable any time)

```bash
# Targeted suite (storage-items + entity-list + entity-service + lookup-table)
cd frontend && pnpm exec jest --testPathPattern="lookup-table|entity-service|entity-list|storage-items"

# Full repo suite (validates no regressions)
cd frontend && pnpm exec jest

# TSC check (decoder: 2 pre-existing errors in builder.page.ts are NOT from TZ-232.C)
cd frontend && pnpm exec tsc -p tsconfig.app.json --noEmit

# ESLint integration (storage-items compliant)
cd frontend && pnpm exec eslint src/app/pages/inventory/storage-items.page.ts
```

## Cross-TZ detection for successors

`grep -lR "PiEntityListComponent\|toEntityService" frontend/src/app` — finds migrated pages. As of 2026-07-28: only `storage-items.page.ts` (POC).
