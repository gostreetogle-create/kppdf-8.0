# TZ-185 — Add barrel `index.ts` to backend modules (Auth + Identity)

**Дата:** 2026-07-25
**Тип:** Refactor + tree-shaking hygiene
**Priority:** ⚪ LOW
**Status:** ⏳ READY
**Layer:** 4 (Backend module structure)
**Agent Role:** Backend refactor engineer
**Estimated complexity:** S (<1 day)

## 1. Контекст

Audit: 73 modules в `backend/src/modules/*`, **0** barrel `index.ts` files. Это значит:
- External импорты verbose (e.g. `backend/src/modules/auth/services/auth.service` vs short `backend/src/modules/auth`).
- Tree-shaking sub-optimal (TypeScript compiler не агрессивно inlines без explicit barrel).
- Refactor (TZ-180, TZ-181, TZ-182) тяжелее без barrel re-exports.

## 2. Проблема

**Specific gap:** `backend/src/modules/{auth,users,permissions,role,counterparty,organization,person}/` — none have `index.ts`.

## 3. Файлы

### Files to create
- `backend/src/modules/auth/index.ts` (barrel).
- `backend/src/modules/users/index.ts`.
- `backend/src/modules/permissions/index.ts`.
- `backend/src/modules/role/index.ts`.
- `backend/src/modules/counterparty/index.ts`.
- `backend/src/modules/organization/index.ts`.
- `backend/src/modules/person/index.ts`.

### Barrel pattern (each)
```typescript
export { AuthModule } from './auth.module';
export { AuthService } from './auth.service';
export { AuthController } from './auth.controller';
// DTOs
export * from './dto';
// Types (if any)
```

### Files NOT to modify
- Existing import statements в callers (out of scope, separate refactor TZ).
- Module internals.

## 4. Action Steps

1. For each module из списка — read source structure, identify exports.
2. Create `index.ts` aggregating.
3. Verify barrel compilable (typecheck pass) without import-pattern changes.

## 5. Acceptance Criteria

1. `find backend/src/modules -name "index.ts"` returns ≥7 (was 0).
2. `pnpm exec tsc --noEmit` exit 0 — barrels compile clean.
3. Barrel content matches actual exports (no ghosts).

## 6. Cross-references

- **Расширяет:** User Audit F (backend hygiene).
- **Nice-to-have для:** TZ-180, TZ-181, TZ-182 (cleaner imports для new tests).

## 7. Out of Scope

- Caller import-pattern updates.
- Auto-generation tooling.
- Wider sweep до 73 modules (separate TZ по batch-domain).

## 8. Finalization

TZF-00 standard flow.
