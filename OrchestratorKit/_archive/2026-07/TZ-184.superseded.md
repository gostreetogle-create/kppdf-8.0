# TZ-184 — Frontend unit test coverage: Auth + Pi-Table (foundation batch)

**Дата:** 2026-07-25
**Тип:** Test coverage gap fill
**Priority:** 🔵 MEDIUM
**Status:** ⏳ READY
**Layer:** 1-2 (Frontend Tests)
**Agent Role:** Frontend QA engineer
**Estimated complexity:** M (1-3 days)

## 1. Контекст

Audit: 199 frontend component files vs 49 spec files = **75% gap**. Among existing 49 specs, **no coverage** for:
- `core/auth.service.ts` (12K lines most-touched protected code).
- `shared/ui/pi-table.component.ts` (CENTRAL reusable primitive, affected by TZ-176/177/178).
- Critical signal-based page patterns.

TZ-176 (pi-table migration) невозможно верифицировать без unit tests для pi-table.

## 2. Проблема

| File | Lines | Test priority |
|---|---|---|
| `core/auth.service.ts` | (verify line count) | P0 — auth & login |
| `shared/ui/pi-table.component.ts` | ~700 | P0 — TZ-176 dependency |
| `shared/services/pi-cost-calculations.service.ts` | (verify) | P1 — has 1 .spec.ts done in TZ-83 |
| Other 150 components | varies | P2 onwards (deferred) |

## 3. Файлы

### Files to create
- `frontend/src/app/core/auth/auth.service.spec.ts` (or wherever AuthService lives).
- `frontend/src/app/shared/ui/pi-table.component.spec.ts` (likely exists — verify; otherwise create).
- `frontend/src/app/shared/services/pi-text-blocks.service.spec.ts` (P1).
- `frontend/src/app/shared/services/pi-table-templates.service.spec.ts` (P1).
- `frontend/src/app/core/interceptors/auth.interceptor.spec.ts`.

### Files NOT to modify
- Existing 49 spec files.

## 4. Action Steps

1. Confirm `frontend/jest.config.js` + setup adequate.
2. Verify SPEC-binding для `pi-table.component` — если missing, recreate per Angular 20 signals.
3. For AuthService: cover login flow, token refresh, logout bump-version, 401 interceptor retry.
4. For interceptor: verify 401 triggers refresh + retry logic with mocked HttpClient.
5. For pi-table: test sortKey sync, page signal update, dialog emit chain.

## 5. Acceptance Criteria

1. `find frontend/src -name "*.spec.ts" | wc -l` ≥ 53 (was 49; add ≥4 new).
2. `pnpm exec jest` exit 0 (existing + new).
3. Coverage: `pnpm exec jest --coverage` reports ≥50% on `auth.service.ts`, `pi-table.component.ts`.
4. New specs runnable standalone.

## 6. Cross-references

- **Foundation для:** TZ-176, TZ-177, TZ-178.
- **Расширяет:** User Audit C.

## 7. Out of Scope

- 150 components P2 coverage (separate TZ, batch-domain likely 30+ components each).
- E2E (Playwright/Cypress) — separate future TZ.

## 8. Finalization

TZF-00 standard flow.
