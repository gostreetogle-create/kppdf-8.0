# Lock file
Locked as of 2026-07-28 — TZ-232.A: Lookup Table rewrite (DestroyRef cleanup).

Owner: 
Protected files:
  (см. CONFLICT KEYS в TZ-232.A.done.txt)

Unlock: only via orchestrator approval (новое TZ-NN с пометкой «successor»).
