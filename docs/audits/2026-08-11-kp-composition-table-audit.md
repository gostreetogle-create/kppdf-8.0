# Audit: «Состав КП» — таблица вместо кучи карточек (2026-08-11)

**Стенд / зона:** Create КП `/proposals/create` · правый flyout «Состав»  
**PO:** глаза от карточной кучи на ~20rem; нужна половина экрана + табличный стиль; правка qty/цены здесь; A4 = превью; «Редактировать» без ухода со страницы.

## Verdict

Состав **функционально** (340/342/352) работал, но UI — **узкий стек карточек**. Витрина «Товары» уже wide (~58rem); состав сидел на **20rem**. После 355 — wide table; после **355.1** (скрин PO) — denser + без моргания A4.

## Канон (TZ-SALES-355 → 355.1)

| Было | Стало |
|------|--------|
| Flyout ~20rem, card heap | `min(50vw, 52rem)`, плотная **таблица** |
| Моргание A4 на qty/цену | Старое превью держится, пока новый build не готов |
| «База» + 3 поля в ячейке имени | Имя + артикул + описание |
| Крупные input vs мелкие иконки | Единый compact (~1.35–1.45rem) |
| Сумма read-only | Сумма правится → цена **только в КП** |
| Copy непонятен | Убран из UI |
| Стрелки в куче справа | ↑↓ вертикально **слева** |
| Фото после edit не видно | Re-fetch product с populate photoIds |

## Не трогали

Shell rails|center|rails · A4 build/PDF · статусы · витрина · DTO quotations.

## Как агенту подходить к таким темам

1. Одна фраза «экран должен…» → аудит → прецедент рядом.
2. Layout → CTA → фичи; не сыпать поля.
3. После скрина PO — второй тонкий проход, не оправдываться.

## Deploy

По команде PO («деплой»).
