# Карта дыр Create КП — почему PO «повторяется»

**Дата:** 2026-08-09  
**После:** 317…332 DONE (студия, bind, наценка/НДС, rail Таблица)

---

## 1. Не баг «сломалось» — ещё не делали

| Боль PO | Сейчас | Нужная TZ |
|---------|--------|-----------|
| F5 «ничего не сохранилось» | Save = **кнопка** «Сохранить», не autosave; без клика F5 пусто | **333** (уже на main) — сначала Save, потом F5 |
| «Редактировать» = странный диалог | `ProposalFormDialog` со списка | **338** → всегда `/proposals/create?id=` |
| EN в подсказках (`draft`) | leak в inspector | **338** RU copy |
| Клиент из списка | Заглушка | **334** все Counterparty + поиск, без фильтра роли |
| Нет колонки Кол-во / Цена на листе | keys нет на бланке | **335** |
| Нет фото в таблице | `previewLines` без photo | **335** |
| Замок / оплачено / копия | не wired | **336** |
| НДС в Параметрах | к Organization (наша фирма) на КП | ок (не баг); не переносить на клиента без новой TZ |

Таблица UI (←→, Видна) — **сделано** (332). Нет keys → 335, не «Видна сломана».

---

## 2. Уже работает (не просить снова)

Шаблон на листе · товары в витрину · строки в target-table · наценка/НДС footer · панель Таблица/Параметры · multi-table selector · Save draft (кнопка).

---

## 3. Волна «хватит повторяться»

`WAVE-KP-USABLE` → 337 DONE → 333 archive → **338** → 334 → 335 → 336.  
Промпт: `PROMPT-KP-USABLE-CONTINUOUS.md`.
