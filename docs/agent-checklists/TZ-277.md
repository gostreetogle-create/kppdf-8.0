# TZ-277 checklist

- Status before work: active task, after TZ-274 commit `43d752a`; Team Room claimed with no reported owner conflict.
- Conflict keys: admin pages/dialogs and shared `PiRowActions`; no backend changes.
- Lifecycle decision: existing page flow closes form dialogs before issuing HTTP, which cannot keep the dialog open on API error. Form dialogs will own create/edit/reset HTTP mutations and close only on success; pages will reload after successful dialog result. Row confirmation mutations remain page-owned with row-level loading.
- Acceptance criteria:
  - Form submit buttons show submitting state and block repeat submit.
  - API error leaves form dialog open with error message and clears submitting.
  - Success closes dialog and reloads list.
  - Row mutations expose loading state for only the active row and block repeat actions.
  - Regression tests cover success/error/double-submit and row loading release.
- Planned checks: targeted admin/dialog/row-action Jest; frontend typecheck; development build; diff-check; independent review; browser only if live authenticated stack is available.
- Finalization: archive under exact Team Room evidence filename, update STATUS/progress, create lock, remove active task only after Team Room complete, then atomic commit.
- Closed 2026-08-02: Team Room complete accepted `tasks/_archive/2026-08/TZ-277-admin-mutation-loading-states.done.md`; active task removed after completion.
- Verification: targeted Jest 6 suites / 58 tests PASS; frontend typecheck PASS; development build PASS; targeted ESLint 0 errors with 2 existing raw-HttpClient architecture warnings; git diff --check PASS; independent review had no critical or important findings.
- Browser: MANUAL_BROWSER_CHECK_REQUIRED (no live authenticated browser flow was run).
- Scope note: backend endpoints unchanged; unrelated working-tree files were not included.
