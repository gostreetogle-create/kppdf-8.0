# Session close-board checklist
- [x] TZ-UI-TABLE-305 CLAIM → code → gates → ARCHIVE → commit → push
- [x] TZ-UI-TABLE-303 CLAIM → code → gates → ARCHIVE → commit → push
- [x] _active/ empty
- [x] _active-map: queue empty / READY FOR DEPLOY
- [x] Final message to PO: «очередь пуста — можно тёплый деплой»

OUT OF SCOPE (не трогать, не спрашивать):
- TZ-UI-TABLE-304 (Selectable/dense — backlog склада)
- deploy.ps1 / wipe / VPN
- backend schema migrations prod apply
