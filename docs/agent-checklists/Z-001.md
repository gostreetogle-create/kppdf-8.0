# Z-001 — agent checklist (executor)

**Task:** `tasks/Z-001-inventory-write-transactions.md`  
**Role:** Backend Engineer (local / Gemini). **Not Cursor** (Mode A = architect/spec only).  
**Created:** 2026-08-02 (Cursor spec upgrade + activation)

## Before start

- [ ] Read `docs/AI-AGENT-GUIDE.md`, `GEMINI.md`, task file end-to-end
- [ ] `git status --short` — no half-baked peer edits on CONFLICT KEYS
- [ ] Do **not** run parallel with `TZ-MATERIALS-308` (shared `stock-movement.service.ts`)
- [ ] Claim / lock: `.mimocode/locks/Z-001-inventory-write-transactions.lock`

## Acceptance tracking

- [ ] External session on `stock-movement.create` + `reservation.fulfill` (no nested txn)
- [ ] `shipment.dispatch` / `purchase-order.receive` / `order.ship` in SessionRunner
- [ ] SessionRunner providers on ShipmentModule + PurchaseOrderModule
- [ ] `stock-movement.remove` policy **(a)** reverse delta + test
- [ ] Rollback unit tests (dispatch / receive / ship / remove)
- [ ] `pnpm exec tsc -p tsconfig.build.json --noEmit` exit 0
- [ ] Targeted + full backend jest PASS
- [ ] `docs/data-model.md` Concurrency & Transactions + ARCHITECTURE.md note
- [ ] Archive DONE + progress.md; push NO

## One-liner for PO → local agent

```
Прочитай docs/AI-AGENT-GUIDE.md, GEMINI.md и tasks/Z-001-inventory-write-transactions.md. Выполни Z-001 (backend only). Frontend не трогать. Push не делать. Checklist: docs/agent-checklists/Z-001.md.
```

## Audit notes (Cursor 2026-08-02)

- Coordinates: dispatch `:99`, receive `:108` (signature `receive(id)` only), ship `:150` — verified.
- Nested own-txn in `stock-movement.create` and `reservation.fulfill` — must fix before outer wrap is atomic.
- dispatch status is `in_transit`, not `shipped` (spec drift documented in task).
