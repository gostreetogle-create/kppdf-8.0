# TZ-274 checklist

- Status before work: active untracked task; TZ-276 superseded separately; no TZ-274 production changes yet.
- Team Room: claimed by canonical main agent; no active conflict reported.
- Acceptance criteria:
  - Users create/toggle/edit require `user:write`: PASS.
  - Users reset-password/delete require `user:admin`: PASS.
  - Roles create/edit require `role:write`: PASS.
  - Roles delete require `role:admin`: PASS.
  - Unauthorized action buttons absent from DOM via `@if`/`showDelete`: PASS.
  - Unit tests cover visible and absent states: PASS.
- Implementation: existing `CapabilitiesService` injected into both admin pages; shared `PiRowActionsComponent` gained backwards-compatible `showDelete` input defaulting to `true`.
- Verification:
  - targeted frontend Jest: PASS, 3 suites / 29 tests.
  - frontend typecheck (`pnpm exec tsc -p tsconfig.app.json --noEmit`): PASS, exit 0.
  - frontend development build (`pnpm exec ng build --configuration=development`): PASS, exit 0.
  - `git diff --check`: PASS, exit 0 (CRLF normalization warnings only).
  - independent review: PASS with one minor direct `showDelete=false` test added and reverified.
  - browser: MANUAL_BROWSER_CHECK_REQUIRED; no live authenticated browser flow was run.
- Finalization: archive marker and status/progress updates pending the final explicit archive/commit step.
