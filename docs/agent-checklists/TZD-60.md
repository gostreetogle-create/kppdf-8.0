# TZD-60 checklist

> Status: **DEFERRED** (не DONE — ШАГ 1 невыполним в этой сессии)
> Marker: `tasks/_active/TZD-60.md` — не создавался: TZ переведена в DEFERRED до кода
> Lock: **не создавался** (по конвенции lock только для outcome DONE)

## Claim slot

- agent_id: `claude-computer`
- claimed_at: 2026-08-22T20:00:00+03:00
- workspace: D:\kppdf-8.0
- team_room_claim: unavailable _(Team Room CLI недоступен в этой сессии)_

## Conflict keys (не тронуты)

- `desktop/src-tauri/windows/hooks.nsh` — не менялся
- `desktop/src-tauri/tauri.conf.json` — не менялся
- `desktop/docs/INSTALL.md` — не менялся в рамках TZD-60 _(правился в TZD-60-независимой TZD-61: раздел «С чего начать» + подписи вкладок; SmartScreen-секция из ШАГ 3 сознательно НЕ добавлялась, т.к. привязана к результату ШАГ 1)_

## Почему DEFERRED

КРИТЕРИЙ ПРИЁМКИ 1 требует дословный лог/скриншоты установки на чистой машине; без него
ШАГ 2–4 по определению TZ остаются гипотезой. Выполнить ШАГ 1 в этой сессии нельзя:

1. Артефакт есть: `desktop/src-tauri/target/release/bundle/nsis/KPPDF Desktop_0.5.6_x64-setup.exe`
   (42 138 073 B, 2026-08-22 17:35) + копии в `frontend/downloads/`. Проблема не в сборке.
2. Доступ к Windows-хосту — только неинтерактивный PowerShell (stdout/stderr). Экрана нет,
   ввода в GUI нет. NSIS сообщает об ошибках диалоговыми окнами; в silent-режиме (`/S`)
   диалоги подавляются, их текст в лог не попадает → «дословно» зафиксировать нечем.
3. ШАГ 1 требует чистое окружение (деинсталляция текущей версии) — разрушающее действие
   на рабочей машине PO. В этой волне разрешён только push; установка/деинсталляция софта
   не разрешалась.
4. Побочно: `pnpm` и `cargo` не в PATH хоста, `child_process.spawn` возвращает `EPERM` для
   любого бинаря → пересобрать инсталлятор в этой сессии тоже нельзя (для ШАГ 1 не нужно).

Гипотетические правки (`nsExec::ExecToLog` в `hooks.nsh`, `webviewInstallMode`) НЕ внесены
осознанно: TZ прямо запрещает выдавать гипотезу за фикс.

## Что нужно для закрытия (для PO)

- **Вариант A (рекомендуется):** запустить `KPPDF Desktop_0.5.6_x64-setup.exe` на чистой
  машине (или после деинсталляции) и прислать дословный текст каждого окна в порядке
  появления. ШАГ 2/3/4 после этого закрываются одним проходом.
- **Вариант B:** разрешить агенту деинсталляцию + установку на этой машине, приняв, что
  вместо дословных диалогов будет только NSIS install-log (`/S` + лог) — AC1 закроется частично.
- Риск варианта B: текст части окон (в т.ч. SmartScreen, который показывает ОС, а не
  инсталлятор) не попадёт в лог вообще.
- Рекомендация: A.

## Открытый вопрос PO (вне scope TZ, из статики)

`desktop/src-tauri/tauri.conf.json` → `bundle.windows.nsis` содержит только `installerHooks`;
`signCommand` / `certificateThumbprint` отсутствуют → инсталлятор не подписан. Это
вероятный источник «Windows защитил ваш компьютер», которое пользователь читает как ошибку.
Покупка/подключение code-signing сертификата — бюджетное решение PO, отдельная TZ.

## Gates

Не запускались: код не менялся (нулевой диф). `tsc`/`svelte-check` по desktop прогонялись
в этой же сессии в рамках TZD-61 — PASS (см. `docs/agent-checklists/TZD-61.md`).

## Closeout

- [x] archive `tasks/_archive/2026-08/TZD-60.deferred.md` (полный текст TZ + ARCHIVE_MARKER, outcome: DEFERRED)
- [x] lock НЕ создавался (outcome ≠ DONE)
- [x] `tasks/TZD-60-desktop-installer-errors-hardening.md` удалён из корня `tasks/` — переоткрывать из archive
- [x] Status = DEFERRED, запись в `_NOW.md` + `progress.md`
- closed_at: 2026-08-22T20:05:00+03:00
