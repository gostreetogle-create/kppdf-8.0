# TZ-NX-DOCSTUDIO-S12-OPERATOR-DOCS

Status: DONE

S12 documentation is synchronized with the implemented behavior:
- document background selection and opacity are revision-gated;
- page margins are persisted/rendered in millimetres;
- sheet row capacities are inherited from templates and used by multipage planning;
- order selection cascades to client only when client is empty;
- table sum/total footers exclude disabled rows;
- Ctrl+Z/three-way merge remains the only active PARK item.
