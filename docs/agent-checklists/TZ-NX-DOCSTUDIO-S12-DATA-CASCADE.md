# TZ-NX-DOCSTUDIO-S12-DATA-CASCADE

Status: DONE

- Selecting an order now fills the client from `order.counterpartyId` only when no client is already selected.
- Existing client selection is preserved.
- Gates: Studio tests PASS (54 suites, 294 passed); `nx build kppdf-web` PASS.
