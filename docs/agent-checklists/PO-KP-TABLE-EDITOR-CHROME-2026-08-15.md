# PO checklist — Редактор таблицы КП (chrome + строка)

> Диктовка + скрины 2026-08-15. Экран: `/proposals/create` → Редактор таблицы.
> Исполнение: **TZ-SALES-374** (один Layer-3 файл table-editor + sheetLayout).

## Список задач (порядок)

| # | Задача | Зачем |
|---|--------|--------|
| 1 | **Рамка / Шапка → иконки** | Текст «Рамка: Жирная / Шапка: Жирный» убрать; цикл/toggle через Lucide + `title`/`aria-label` с состоянием |
| 2 | **Два шрифта: шапка и тело** | Сейчас один «Шрифт» на всю таблицу. Нужно: размер **шапки** отдельно от **ячеек** (оба 8…20, Paper & Ink overflow-select). Сохранить в `sheetLayout` |
| 3 | **Правый жёлоб строки упростить** | На строке оставить **только стрелку** раскрытия drawer. Карандаш / ⋯ / корзина → **внутрь панели** строки |
| 4 | **Подписи действий понятнее** | «Создать копию изделия» → ясно: копия **в каталоге** (новый Product), не путать с «Дублировать строку КП» |
| 5 | **Рамка раскрытой строки** | Как UX-319 на `/products`: товар+drawer обведены ink-рамкой; соседние строки приглушены |
| 6 | **Тулбар компактнее** | Где можно — иконки вместо длинных подписей; без help-простыней на экране |

## Не путать

- Документы → шаблон таблицы — **не** этот экран.
- Per-column шрифт — **не** в этой волне.

## Статус

- [x] Чеклист
- [x] TZ-SALES-374 → Buffy (claimed)
- [ ] Cursor PASS → archive
