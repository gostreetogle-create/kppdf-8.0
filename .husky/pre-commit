pnpm run lint-staged

# SUPPLY-GATE: быстрый focused Jest + (если стенд :3000 поднят) полный
# supply-smoke по затронутым файлам контура снабжения/склада.
node scripts/smoke/supply-gate.mjs

# Полная проверка (tsc + ng build + jest) — НЕ в hot-path pre-commit:
# слишком медленно. Запускается вручную:
#   pnpm --dir frontend build:dev
#   (или скилл .agents/skills/run-project-checks/SKILL.md)
# tsc --noEmit НЕ проверяет Angular templates — только ng build их компилирует.
