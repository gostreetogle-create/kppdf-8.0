# TZ-DOC-TABLES-308: Диалог таблицы — баланс source/fields + живой превью низ

PAGES: /doc-constructor/tables  
PAGE_DOCS: tables.page.md  
PO visual 2026-08-09 (скрин «Редактировать шаблон таблицы»): поля источника слишком широкие; шапки столбцов сжаты; низ диалога — пустое серое море без эталона таблицы.

РОЛЬ АГЕНТА: frontend  
ЗАВИСИМОСТИ: TZ-DOC-TABLES-305 DONE (archive); не пересекать 306 chip-fix без нужды  
LAYER: 3  
CONFLICT KEYS: frontend/src/app/pages/doc-constructor/tables/table-template-dialog.component.ts; frontend/src/app/pages/doc-constructor/tables/table-template-dialog.component.spec.ts; docs/pages/tables.page.md

Проверено: скрин PO; `.ttd-field--source` / `--fields` ширины; `.ttd-ih` шапки; `ttd-main` min-height + пустой tbody когда нет preview rows; hint «Данные будут подставлены…».

═══════════════════════════════════════════════════════════════
ЧТО ДЕЛАТЬ
═══════════════════════════════════════════════════════════════

1. **Строка источник + поля** — на одной линии, **сопоставимые ширины** (не ⅓ vs ⅔). Цель: ~1:1 или source ≤~40% / fields ≥~60% max, но fields не «на всю оставшуюся пустоту» визуально доминирует. Wrap на узком окне ок.
2. **Шапки столбцов (`.ttd-ih`)** — заметно выше (padding/min-height), ключ+лейбл читаются без «сжатой полоски».
3. **Низ диалога** — не мёртвая пустота:
   - **Предпочтительно:** всегда показывать **эталон-превью** таблицы (thead + 1–2 sample/skeleton rows) в зоне под шапками — «как будет выглядеть в документе»;  
   - либо если sample нет — явный skeleton row(s), не огромный пустой flex.
4. Уменьшить ощущение «серого моря»: `min-height` main не раздувать зря; превью занимает доступную высоту.
5. Jest smoke + `tables.page.md` одна секция layout/preview.
6. Gates: FE tsc + dialog spec PASS.

═══════════════════════════════════════════════════════════════
НЕ ИЗМЕНЯТЬ
═══════════════════════════════════════════════════════════════

- Enum типов / 307 preset KP
- Chip «Из данных» (306)
- BE registry schema sync
- deploy

═══════════════════════════════════════════════════════════════
КРИТЕРИИ ПРИЁМКИ
═══════════════════════════════════════════════════════════════

1. Source и fields визуально на одной строке, fields не «простыня на ⅔».
2. Шапки столбцов выше / воздушнее (PO eyeball).
3. Под шапками виден эталон таблицы (строки или skeleton), не пустой низ на пол-диалога.
4. tsc + focused dialog tests PASS.
