# QA-445F: Стол → Состав заказа — клик по строке сразу входит в редактирование

> **BLOCKED до архивации `TZ-UX-444A`.** Conflict keys пересекаются
> (order-detail page/spec, см. `tasks/_active/TZ-UX-444A.md`). Не брать в
> работу параллельно — сверить `docs/agent-checklists/_NOW.md`, что 444A
> закрыт, перед Claim этой TZ.

РОЛЬ: frontend.

CONFLICT KEYS: order-detail / manager-desk order-tray composition-list
(уточнить при Claim, какой именно компонент — `OrderHubTrayComponent` в
`pages/desk/manager-desk.page.ts` или `pages/orders/order-detail.page.ts`,
по скриншоту это вкладка «Стол» внутри карточки заказа, значит вероятнее
desk-tray, но нужно свериться на живом коде, т.к. и order-detail сейчас
меняется в 444A).

ЧТО: В блоке «Состав заказа» (строка «Навес АКП ДПС», значение «3» + иконка
карандаша справа) PO обвёл всю строку целиком и написал: «при нажатии на
поле закидывает на редактирование — нужно через карандаш редактирование»
(16:54). То есть сейчас клик в любом месте строки (не только по иконке
карандаша) открывает режим редактирования количества/позиции — ожидание PO:
редактирование должно открываться **только** по клику на иконку карандаша,
а клик по остальной части строки — либо ничего не делает, либо выполняет
другое действие (раскрытие/переход), но не редактирование.

НЕ: не трогать сам механизм редактирования (что именно редактируется и как
сохраняется) — только точку входа (какой клик его открывает). Не трогать
`app-pi-status-banner`/сам статус заказа — это зона 444A.

AC: клик по иконке карандаша в строке состава заказа открывает
редактирование; клик по остальной площади строки — не открывает
редактирование (согласовать с PO/архитектором, что должно происходить
вместо этого — ничего, или раскрытие деталей строки).
