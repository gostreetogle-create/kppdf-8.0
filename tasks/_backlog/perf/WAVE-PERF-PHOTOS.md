# WAVE-PERF-PHOTOS — быстрые списки без тяжёлых фото

**Статус:** ACTIVE — `TZ-PHOTO-301` DONE; следующий `TZ-PHOTO-302` READY; `TZ-PHOTO-303` blocked until 301 and 302 are DONE.

**Зачем простыми словами:** страница каталога тормозит, потому что в списке
показываются **полные** фото (как с телефона). Нужно: при загрузке фото сервер
сам делает **лёгкую копию** для списков, оригинал хранит отдельно. Бизнес-логика,
дизайн и смысл экранов не меняются — только скорость.

**Порядок (строго по слоям, не параллелить 301∥302):**

| # | TZ | Что получит PO | Кто |
|---|-----|----------------|-----|
| 1 | **TZ-PHOTO-301** | При загрузке фото сервер сохраняет оригинал + лёгкое превью | ✅ DONE — archive + push |
| 2 | **TZ-PHOTO-302** | Все списки/карточки каталога берут превью, не оригинал | **READY — next** |
| 3 | **TZ-PHOTO-303** | Старые фото (уже в базе) тоже получают превью | executor |

**Уже сделано вне волны:** TZ-UX-314 — на рабочих списках по **10** строк/карточек
(меньше картинок за раз).

**Не входит:** смена дизайна карточек, сжатие «в ноль», удаление оригиналов,
CDN/S3.

**Continuous prompt:**

```text
Прочитай GEMINI.md + tasks/_backlog/perf/WAVE-PERF-PHOTOS.md.
Очередь: TZ-PHOTO-301 → 302 → 303 (по DEPENDS ON).
CLAIM → код → gates → archive → commit/push → следующий.
Пикеры/бизнес-логику не трогать. Deploy только по явной команде PO.
```
