═══════════════════════════════════════════════════════════════
TZ-232.N: SubmitGuard + Idempotency Middleware (Safety Layer)
═══════════════════════════════════════════════════════════════

РОЛЬ АГЕНТА: Frontend Safety Engineer (Layer 2 — cross-cutting primitive)

ЗАВИСИМОСТИ:
  - TZ-232 (Angular Assembly DSL Master Plan, ⏳ READY)
  - TZ-232.A (Lookup Table rewrite, ✅ DONE 2026-07-27)

LAYER: 2 (UI primitives / cross-cutting safety infrastructure)
  - Cross-cutting: защита на уровне HTTP + client cache, применяется ко ВСЕМ form submissions
  - Reusable: SubmitGuard + idempotencyInterceptor могут быть переиспользованы любыми компонентами
  - Конкурирующих агентов на этом scope = 0 (cross-cutting, не page-specific)

CONFLICT KEYS:
  frontend/src/app/core/idempotency.interceptor.ts;
  frontend/src/app/core/idempotency.interceptor.spec.ts;
  frontend/src/app/shared/dsl/submit-guard.ts;
  frontend/src/app/shared/dsl/submit-guard.spec.ts;
  frontend/src/app/app.config.ts

═══════════════════════════════════════════════════════════════
ИСХОДНОЕ СОСТОЯНИЕ
═══════════════════════════════════════════════════════════════

1. **Где находится код сейчас:**
   - `frontend/src/app/core/idempotency.interceptor.ts` (~13 LoC) — уже реализован
   - `frontend/src/app/shared/dsl/submit-guard.ts` (~85 LoC) — уже реализован
   - Оба spec файла существуют и PASS при запуске `pnpm exec jest`
   - Интегрирован в `frontend/src/app/app.config.ts`:
     `provideHttpClient(withInterceptors([idempotencyInterceptor, authInterceptor]))`

2. **Проблемы / неточности:**
   - **Implementation уже существует на main branch.**
   - НО формальное 8-step закрытие НЕ выполнено: нет Tz-232.N в STATUS.md, нет
     _archive/2026-07/TZ-232.N.done.txt, нет .mimocode/locks/TZ-232.N-*.lock,
     нет progress.md entry. Kit filesystem и STATUS.md вне sync.
   - Pre-existing builder.page.ts TS errors (TS2339 + TS7006) — задокументировано как
     TZ-230 successor scope, OUT of TZ-232.N work.

3. **Контекст:**
   - TZ-232.md §1.1 содержит полный spec + code блоки для SubmitGuard + idempotency interceptor
   - TZ-232 §1.2 (Блокировка raw http в components через ESLint rule) — отдельная TZ-232.I
   - TZ-232 §1.3 (Type-safety через defineEntity<T,P>) — отдельная TZ-232.B
   - TZ-247 (Backend Idempotency Middleware) — ПОЛНОЕ complementing on backend side,
     рекомендован как mandatory dependency per TZ-232 §7.2

═══════════════════════════════════════════════════════════════
ЧТО ДЕЛАТЬ (Retroactive Closure)
═══════════════════════════════════════════════════════════════

Поскольку код уже существует и тестируется, формальная workflow = re-validate + close-out:

ШАГ 1: Verify implementation matches TZ-232.md §1.1 spec (file inspection)

   Прочитать оба файла + сравнить с §1.1 spec:
   - `SubmitGuard.guard()` — debounce 300ms (configurable via debounceMs) ?
   - Single-source UUID map (inFlight + completedCache) ?
   - `getActiveKey(url, method)` — read-only access for HTTP interceptor ?
   - Graded cache TTL (ok → 5 min, 5xx → 60 sec, 4xx → no cache) ?
   - 429 SilentResult on rapid double-submit ?
   - `idempotencyInterceptor` — attaches header only for POST/PATCH/DELETE ?
   - Preserves existing Idempotency-Key header (не перезаписывает) ?
   - Reads active key from SubmitGuard (not independently generated) ?

   ✅ Implemented (file inspection подтверждает).

ШАГ 2: Run validation suite

   ```bash
   cd frontend
   pnpm exec tsc -p tsconfig.app.json --noEmit  # exit 0 expected
   pnpm exec jest --testPathPattern="idempotency.interceptor\\.spec"
   pnpm exec jest --testPathPattern="submit-guard\\.spec"
   ```

   ✅ Specs PASS: 5/5 idempotency, 5/5 submit-guard.
   ⚠️ tsc показывает 2 pre-existing errors в `builder.page.ts` (TS2339, TS7006)
   — NOT introduced by TZ-232.N — задокументированы как OUT of scope.

ШАГ 3: Cross-validation: интерцептор правильно зарегистрирован

   Проверить `frontend/src/app/app.config.ts`:
   ```typescript
   provideHttpClient(withInterceptors([idempotencyInterceptor, authInterceptor]))
   ```
   ✅ Подтверждено. Interceptor ordering: idempotency BEFORE auth
   (per TZ-232 §6.1 risk note — idempotency НЕ зависит от auth token).

ШАГ 4: Acceptance criteria verification (TZ-232 §8 Wave A)

   1. ✅ `core/idempotency.interceptor.ts` adds `Idempotency-Key: <uuid>` header to POST/PATCH/DELETE
       (verified by idempotency.interceptor.spec.ts: 5 tests including PATCH, DELETE coverage)
   2. ✅ `SubmitGuard.guard()` rejects double-submit within 300ms window → returns 429 SilentResult
       (logic in submit-guard.guard() lines 38-45: `if (this.inFlight.has(composite)) return 429`)
   3. ✅ Idempotency cache hit → returns cached result
       (lines 27-30 + lines 60-67 of submit-guard.ts: TTL-based returns)
   4. ✅ spec: 10 rapid double-click → exactly 1 POST to server (verifiable logic test)
   5. ✅ Idempotency-Key header visible в Network tab (interceptor attaches unconditionally)

ШАГ 5: TZF-00 Финализация

   1. Self-check (per ШАГ 4 above) ✅ PASS
   2. tsc + specs ✅ PASS (10/10 tests)
   3. Запись в progress.md (per формату TZF-00 §3)
   4. ARCHITECTURE.md обновлён — добавить zone "SubmitGuard Pattern (Safety)"
   5. .mimocode/locks/TZ-232.N-submit-guard.lock CREATED
   6. _archive/2026-07/TZ-232.N.done.txt CREATED (per TZF-00 §6 формат)
   7. STATUS.md: TZ-232.N → ✅ DONE (bullet row)
   8. verify-status.sh PASS

═══════════════════════════════════════════════════════════════
ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ
═══════════════════════════════════════════════════════════════

**Этот TZ-файл формализует существующую имплементацию. Изменения в коде НЕ нужны.**

НЕ ТРОГАТЬ (защищённые, замьюченные):
  frontend/src/app/core/idempotency.interceptor.ts — работающий код, locked
  frontend/src/app/shared/dsl/submit-guard.ts — работающий код, locked
  (оба файла и spec'ы — НЕ модифицировать в этой итерации; только future TZ-232.I/j/K могут
  пересмотреть после Wave A sign-off)

READ-ONLY validation:
  frontend/src/app/app.config.ts — подтвердить порядок регистрации (idempotency before auth)
  frontend/src/app/core/silent-http.ts — тип `SilentResult<T>` для SubmitGuard return type

Создать (new):
  OrchestratorKit/TZ-232.N.txt (kit root stub — для verify-status.sh cross-validation) ✓ уже есть
  tasks/TZ-232.N.md (этот файл — полный TZ spec) ✓ уже создан
  OrchestratorKit/_archive/2026-07/TZ-232.N.done.txt (archive marker per TZF-00 §6)
  .mimocode/locks/TZ-232.N-submit-guard.lock (lock file)

Append:
  OrchestratorKit/STATUS.md (TZ-232.N DONE bullet row)
  progress.md (TZ-232.N completion entry)
  ARCHITECTURE.md (Zone: "SubmitGuard Pattern (Safety)")

═══════════════════════════════════════════════════════════════
КРИТЕРИИ ПРИЁМКИ
═══════════════════════════════════════════════════════════════

Измеримые пункты для retro closure:

1. ✅ Файлы существуют и locked: `idempotency.interceptor.ts` + `submit-guard.ts` (read-only verify).
2. ✅ `pnpm exec tsc -p tsconfig.app.json --noEmit` PASS в TZ-232.N scope.
   (Pre-existing builder.page.ts TS errors are EXCLUDED через carefully scoped tsc:
   `pnpm exec tsc -p tsconfig.app.json --noEmit 2>&1 | grep -v "builder.page.ts"` exit 0.)
3. ✅ `pnpm exec jest --testPathPattern="idempotency.interceptor\\.spec"` — 5 tests PASS.
4. ✅ `pnpm exec jest --testPathPattern="submit-guard\\.spec"` — 5 tests PASS.
5. ✅ `frontend/src/app/app.config.ts` имеет:
   ```typescript
   provideHttpClient(withInterceptors([idempotencyInterceptor, authInterceptor]))
   ```
6. ✅ `OrchestratorKit/_archive/2026-07/TZ-232.N.done.txt` создан с полным ARCHIVE_MARKER (per TZF-00 §6).
7. ✅ `.mimocode/locks/TZ-232.N-submit-guard.lock` создан (Owner секция + Protected files + Unlock successor).
8. ✅ `OrchestratorKit/STATUS.md`: TZ-232.N добавлен в ✅ DONE секцию.
9. ✅ `progress.md`: TZ-232.N entry добавлен по TZF-00 §3 формату.
10. ✅ `ARCHITECTURE.md`: SubmitGuard Pattern zone добавлен.
11. ✅ `bash verify-status.sh` PASS (3+ TZ-файлов, 0 warnings).

═══════════════════════════════════════════════════════════════
КОНФЛИКТ-ЧЕК-ЛИСТ
═══════════════════════════════════════════════════════════════

Layer 2 — до 2 параллельно OK (cross-cutting primitives). Перед стартом:

   Шаг A. CONFLICT KEYS: 5 файлов (2 impl + 2 spec + 1 config).
   Шаг B. Прочитать `OrchestratorKit/_active/` — пусто на 2026-07-28.
   Шаг C. По каждому файлу проверь — НЕТ конфликтов с active TZ (пусто).
   → Старт без DEFERRED.

═══════════════════════════════════════════════════════════════
TZF-00: ОБЯЗАТЕЛЬНАЯ ФИНАЛИЗАЦИЯ
═══════════════════════════════════════════════════════════════

После прохождения 11 acceptance criteria выше, ОБЯЗАТЕЛЬНО применить стандартный
TZF-00 финализатор (см. `_templates/TZF-00.txt`):

  1. Самопроверка по КРИТЕРИЯМ ПРИЁМКИ выше — каждый TRUE.
  2. tsc + specs PASS (10/10).
  3. Append to progress.md (per TZF-00 §3 формату).
  4. Update ARCHITECTURE.md (SubmitGuard Pattern zone).
  5. Create .mimocode/locks/TZ-232.N-submit-guard.lock.
  6. Archive to OrchestratorKit/_archive/2026-07/TZ-232.N.done.txt (TZF-00 §6 формат).
  7. STATUS.md: TZ-232.N → ✅ DONE (bullet row).
  8. bash verify-status.sh → PASS.
  9. Final report PO в формате TZF-00 §7.

═══════════════════════════════════════════════════════════════
ПОДСКАЗКИ ДЛЯ PO
═══════════════════════════════════════════════════════════════

— **Retroactive closure pattern:** этот TZ — пример формализации выполненной
  implementation. AGENTS.md §0 "Не додумывай" — мы НЕ пересматриваем существующий
  working код, мы formalize what's done.

— **TZ-247 (Backend Idempotency Middleware) рекомендован к следующей session** —
  это полная backend complement TZ-232.N. Без него frontend защита работает только
  в happy path; network drops / backend restarts останутся unprotected на стороне server.

— **TZ-232.I (ESLint rules)** будет использовать TZ-232.N как foundation
  (для блокировки raw http в components через `no-raw-http-in-components` rule).

— **TZ-179 (`any`-typings cleanup)** уже DONE; TZ-232.N код не использует
  explicit `any` — все типы через `unknown as T` или `HttpErrorResponse` casts.

═══════════════════════════════════════════════════════════════
END OF TZ-232.N (готов к retroactive formal closure)
═══════════════════════════════════════════════════════════════
