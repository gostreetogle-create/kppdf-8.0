Ты — ведущий архитектор Angular, специалист по рефакторингу и чистому коду.
Твоя задача — провести архитектурный аудит моего Angular-проекта на основе предоставленного описания и составить максимально подробный список технических задач (TZ) для наведения порядка.

Этот же промпт я отправлю ещё нескольким AI-агентам. Вы работаете параллельно и независимо друг от друга. Не переживай о пересечениях с другими — дублирование идей это плюс, оно поможет собрать полную картину. Позже я объединю все списки в один.

**Твоя миссия:** не вносить изменения в код, а только сформировать структурированный план рефакторинга. План должен следовать лучшим практикам Angular: изоляция модулей, умные/глупые компоненты, управление состоянием, производительность (OnPush), отсутствие утечек памяти, чистая архитектура.

**Информация о моём проекте:**
[ОПИСАНИЕ_ПРОЕКТА]

В этом блоке я описал:
- структуру папок (хотя бы верхний уровень)
- версию Angular и используемые библиотеки (state-менеджмент, UI-кит)
- симптомы проблемы: "когда я что-то меняю в одном месте, ломается в другом", "всё в куче", "трудно добавлять новые фичи"
- примеры нарушений, если известны (огромные компоненты, циклические импорты и т.д.)

**Твой алгоритм действий:**
1. Проанализируй описание и типичные для таких проектов уязвимости.
2. Составь список конкретных задач по наведению порядка. Каждая задача должна быть атомарной (на 2–4 часа работы), чтобы их можно было выполнять постепенно.
3. Сгруппируй задачи по этапам — от самых безопасных структурных изменений до глубокой оптимизации.
4. Для каждой задачи обязательно укажи:
   - **Заголовок** (краткая суть)
   - **Описание** (что именно нужно сделать, какие модули/компоненты затронуть)
   - **Приоритет** (High, Medium, Low)
   - **Этап** (номер этапа по порядку выполнения)
   - **Критерии готовности** (как проверить, что задача выполнена правильно)
   - **Теги** (например, angular, smart-dumb, core-module, state, subscription, onpush)

**Формат ответа:**
Сначала выдай JSON-массив задач, который я смогу легко импортировать в свой таск-менеджер (Todoist, Jira, Trello и т.п.). Пример структуры:
```json
[
  {
    "id": 1,
    "title": "Выделить CoreModule",
    "description": "Создать CoreModule, перенести туда синглтоны (сервисы аутентификации, глобальные перехватчики, guards). Убедиться, что CoreModule импортируется только один раз в AppModule.",
    "priority": "High",
    "stage": 1,
    "acceptance_criteria": "CoreModule содержит только сервисы, объявленные с providedIn: 'root' или в провайдерах модуля; приложение компилируется и работает.",
    "tags": ["angular", "core-module", "singleton"]
  }
]