# PROMPT — Freebuff continuous: QA follow-up closeout

Ты executor (`agent_id: freebuff`). `D:\kppdf-8.0` main · UNATTENDED.

### Preflight
`_active` пуст. Читай `WAVE-MAP.md` в `tasks/_ready/2026-09-17-qa-followup/`.

### Цепочка → STOP
0. `TZ-DOCS-QA-CHECKLIST-CLOSE-AUTH`
1. `TZ-DOCS-QA-REGISTRIES-FIXTURE-CANON`
2. `TZ-DOCS-QA-PROPOSALS-RECLASS`
3. `TZ-NX-SHELL-QUICKNAV-COUNT-PIN`

Каждый: CLAIM → сделай → archive → commit своих файлов → next.

Gates для #3: focused `app-shell.component.spec.ts` + `cd frontend-nx && pnpm exec nx build kppdf-web`.

### НЕ
Deploy · invent registries RBAC · PDF/quotation pipeline · чужой dirty

### STOP
`_active` empty · WAVE-MAP CLOSED · отчёт SHA×4.
