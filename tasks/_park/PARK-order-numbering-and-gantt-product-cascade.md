# Backlog — Order / KP numbering + Gantt product cascade (parked)

**Source:** PO 2026-08-15 + audit `docs/audits/2026-08-15-order-edit-and-gantt-hierarchy-audit.md`

## Parked (do not start until PO reopens)

1. **Short order numbers** — numeric ~3–4 digits, auto +1, manual edit, collision → next free; KP may share display family later.
2. **KP versions** `N.1` / `N.2` labels on freeze (keep snapshot model).
3. **Required deadlines** on create + copy KP production/delivery days on convert.
4. **Gantt cascade** Order → Product → Module? → WorkType (today Order → WorkType flat).
5. **Design queue** before shop (`readyForWork` / `/design`).
6. **Convert polish** — use Quotation.siteId; safe non-product KP lines.

**Unblock first:** TZ-ORDERS-336 (form Save / site / freeze).
