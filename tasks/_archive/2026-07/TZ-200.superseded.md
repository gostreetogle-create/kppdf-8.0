# TZ-200 — SupplierOrder ⇄ PurchaseOrder + RppEntry ⇄ Rpp canonical naming

**Дата:** 2026-07-25
**Тип:** Data-model consolidation (successor-TZ для `docs/data-model-audit.md` Priority 1 #4 + #6)
**Priority:** 🟠 HIGH
**Status:** ⏳ READY
**Layer:** 4 (Backend)
**Agent Role:** Backend data-model engineer
**Estimated complexity:** M (1-3 days)

## 1. Контекст

`docs/data-model-audit.md` §1.1 идентифицирует:
- **#4**: `PurchaseOrder` ⇄ `SupplierOrder` (dual FK: `supplierOrgId` + `supplierId` + дубль `expectedDate`/`deliveryDate`).
- **#6**: `Warehouse.roleIds[]` — должен быть `WarehouseAccess` join-entity.
- **#8**: `Operation` ⇄ `WorkType` — НЕ мерджить, переименовать `Operation` → `RoutingStep` (мнение аудита: разные семантики).

Диагностика (2026-07-25):
- `backend/src/modules/supplier-order/` — НЕ существует. Только `purchase-order/` (canonical).
- `purchase-order.schema.ts:5, 30, 38-77` — единственный canonical schema.
- `PurchaseRequest.service.ts:96-124` имеет explicit `convertToPurchaseOrder` flow.
- `warehouse.schema.ts` НЕ содержит `roleIds[]` (legacy mention in audit был неточный).
- `backend/src/modules/operation/` — НЕ существует. Только `work-type/` (canonical).
- `rpp-entry/` — НЕ существует (только `rpp/`).

## 2. Проблема

Audit baseline указывает на conceptual renamings, но **большинство уже resolved** в code. Реальная работа:
- Cleanup устаревших комментариев в audit-doc, которые ссылаются на несуществующие schemas.
- Audit §1.1 #4/#6/#8 reference — update статус в audit doc как **DONE (already canonical)**.
- Verify что НОВЫЕ legacy-поля (`supplierOrgId`, `expectedDate`) нигде не появились в `purchase-order.schema.ts`.

## 3. Файлы

### Files to modify
- `backend/src/modules/purchase-order/purchase-order.schema.ts` (verify no legacy `supplierOrgId`/`expectedDate`)
- `backend/src/modules/purchase-order/purchase-order.service.ts` (cleanup references to "SupplierOrder" if any)
- `backend/src/modules/rpp/rpp.schema.ts` (verify no legacy "RppEntry" reference)
- `docs/data-model-audit.md` (mark §1.1 #4/#6/#8 как DONE — terminology resolved)
- `ARCHITECTURE.md` §4 (mention status)

### Files NOT to modify
- `backend/src/modules/purchase-order/`
- `backend/src/modules/rpp/`
- Остальные frontend/backend modules

## 4. Action Steps

1. Verify no orphan references: `grep -rn "supplierOrgId\|expectedDate\|SupplierOrder\|RppEntry\|Operation" backend/src/`.
2. Если найдены — apply small replacement (DONE in this TZ).
3. Update audit doc: §1.1 #4 row status = "DONE (was conceptual; resolved at schema level)".
4. §1.1 #6: mark DONE (Warehouse roleIds not actually present; pattern resolved via global RBAC).
5. §1.1 #8: mark DONE (Operation module не существует; WorkType is canonical; renamed to RoutingStep concern deferred).
6. `pnpm exec tsc --noEmit` exit 0 verify.

## 5. Acceptance Criteria

1. `grep -rn "supplierOrgId\|expectedDate\|SupplierOrder\|RppEntry\|\"Operation\"\|\\.Operation" backend/src/` returns 0 hits (except audit-doc references in JSDoc).
2. `docs/data-model-audit.md` has §1.1 #4 + #6 + #8 rows updated to **DONE**.
3. `pnpm exec tsc --noEmit` exit 0 в backend.
4. PurchaseOrder e2e suite PASS.
5. Явный комментарий в `purchase-order.schema.ts:5-7` JSDoc указывает на canonical vs legacy.

## 6. Cross-references

- **Расширяет:** TZ-199 паттерн (rename/mark-DONE в audit doc).
- **Зависит от:** НЕТ — fully independent.
- **Упомянуто в:** `docs/data-model-audit.md` §1.1 #4 + #6 + #8 + §7 (Entities для переименования).

## 7. Out of Scope

- `RoutingStep` schema introduction (audit says оставить как есть).
- WarehouseAccess join-entity (this TZ is **DONE-mark not NEW-introduction**).
- ICategory consolidation (TZ-201 covers broader pattern).

## 8. Finalization

TZF-00 standard flow. Lock: `.mimocode/locks/TZ-200-supplierorder-rppentry-cleanup.lock`.
