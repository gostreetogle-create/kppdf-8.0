# TZ-202 — **SUPERSEDED** (split into TZ-202.A + TZ-202.B per code-reviewer verdict)

**Дата:** 2026-07-25
**Статус:** ⚠️ **SUPERSEDED — DO NOT EXECUTE. Read TZ-202.A + TZ-202.B instead.**

This file is retained **for historical context only**. Per second code-reviewer-minimax-m3 verdict (NEED-FIX #1), the original TZ-202 (which combined AuditLog unification + 3NF cleanup in single scope) was too large.

## Active successors

- **`tasks/TZ-202.A.md`** — AuditLog unification (OrderHistory+UserActivity → AuditLog). Layer 4, MEDIUM.
- **`tasks/TZ-202.B.md`** — 3NF computed-field cleanup (drop `*Name`/`*Sku` caches + Mongoose virtuals for `totalCost`/`balance`/`netProfit` + User.password security fix). Layer 4, MEDIUM (**XL complexity**, **PO sign-off required** per TZ-91 chain).

## What was here before (for archival)

Original TZ-202 tried to cover:
- §4.7 AuditLog unification (OrderHistory + UserActivity → AuditLog)
- §3.1 30+ redundant computed-field cleanups
- User.password security fix
- M2M-attribute AuditLog fields
- 4-entity Mongoose virtual migration (CostCalculation, ReconciliationAct, FinancialReport, etc.)

## Why superseded

The single TZ mixed:
1. Schema-extension work (AuditLog becomes canonical)
2. Data-migration work (OrderHistory rows → AuditLog rows)
3. Computed-field removal (30+ drops across schemas)
4. Mongoose virtual-pattern introduction (4 entities)
5. Security-fix (User.password plaintext removal)

Reviewer verdict: "wrong granularity — split into manageable scopes".

## Finalization note

This file (`tasks/TZ-202.md`) is **superseded and should not be selected by future agents**. Future readers should select:
- `tasks/TZ-202.A.md` for AuditLog unification
- `tasks/TZ-202.B.md` for 3NF cleanup + User.password fix

Lock-файл: `.mimocode/locks/TZ-202.superseded.lock` (read-only marker).

## Cross-references

- **Original draft:** see git history of this file.
- **Split rationale:** code-reviewer-minimax-m3 verdict on TZ-199..202 batch.
- **Tables/exports:** TZ-202.A + TZ-202.B combined replace every action item listed in original TZ-202.
