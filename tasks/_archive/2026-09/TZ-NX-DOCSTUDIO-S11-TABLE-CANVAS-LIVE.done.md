# TZ-NX-DOCSTUDIO-S11-TABLE-CANVAS-LIVE — DONE

The editor now consumes live rows returned with data-set updates and renders them on non-manual table blocks through the existing canvas table display. Manual table editing remains unchanged; source switches continue through the existing revision-gated data-set API.

Validation: `pnpm exec nx build kppdf-web` — PASS.
