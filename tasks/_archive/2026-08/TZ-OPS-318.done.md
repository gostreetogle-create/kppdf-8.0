# TZ-OPS-318 — DONE

Ротация бэкапов в backup.sh + документация volume/cron.

Gates: bash -n PASS (both backup.sh + pre-push).
Known limitations: cron install needs SSH/PO; offsite backup — separate decision.