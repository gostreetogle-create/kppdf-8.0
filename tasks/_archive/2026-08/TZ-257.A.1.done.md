# TZ-257.A.1 — Admin user mutations follow-up — DONE

**Source task:** `tasks/TZ-257.A.1-follow-up.md` (deleted after archive verified)
**Closed at:** 2026-08-01
**Closed by:** autonomous-agent (Codebuff session)
**Outcome:** DONE — all 6 sections implemented, verified, archived.

## Summary

Completed the admin user-management surface that was deferred from TZ-257:

1. **LastAdminGuard demotion gap closed** — `PATCH` with `body.role` demoting the last active admin → 403 `LAST_ADMIN_INVARIANT`. TDZ bug (signal read before declaration) fixed. Unit coverage added for demote-only-admin (rejected) and demote-with-another-admin (allowed).
2. **Administrator password reset** — `AdminResetPasswordDto { newPassword }` (8–128, no `oldPassword`), `UserService.adminResetPassword()` (bcrypt hash + `refreshTokenVersion += 1` + `userActivityCache.invalidate()`), `POST /api/admin/users/:id/reset-password` with `@Permissions('user:admin')` + `@Roles('admin')` + `@UseGuards(LastAdminGuard)` + `@AuditAction('admin.user.password-changed')`, returns `toClientUser()`.
3. **Controller/service tests** — `users-admin.controller.spec.ts` (mutation delegation, redacted responses), `user.service.spec.ts` (no old-password compare, refresh-token invalidation), `last-admin.guard.spec.ts` (PATCH-demote).
4. **Production-order org scope** — `@RequireOrgScope()` + `@UseInterceptors(OrgScopeGuardInterceptor)` applied to `production-order.controller.ts` (the 11th controller; closes the TZ-239.A gap).
5. **Admin users frontend** — `users-admin.page.ts` (create / edit / reset-password / activate / deactivate / delete with confirm, `refresh()` after mutations, `LAST_ADMIN_INVARIANT` 403 → «Нельзя удалить/понизить последнего админа»), `user-form-dialog.component.ts`, `reset-password-dialog.component.ts`, `users-admin.page.spec.ts` (5 tests, deterministic silent-* harness).
6. **STATUS.md sync** — TZ-247 idempotency and TZ-238-241 org-scoping rows corrected to DONE state.

## Verification (real evidence)

```
=== BACKEND ===
pnpm exec tsc -p tsconfig.build.json --noEmit  → exit 0
pnpm exec jest (unit, no e2e)                  → 26 suites, 238/238 PASS

=== FRONTEND ===
pnpm exec tsc -p tsconfig.app.json --noEmit    → exit 0
pnpm exec jest --testPathPattern 'users-admin'  → 5/5 PASS
pnpm exec jest (full)                           → 59 suites, 559/559 PASS
pnpm exec eslint src/app/pages/admin/ --ext .ts → 0 errors (pre-existing raw-http warnings only)

=== GIT ===
git diff --check → clean
```

## Files shipped

| Area | Files |
|---|---|
| Backend guard | `backend/src/common/guards/last-admin.guard.ts` (+spec) |
| Backend DTO | `backend/src/modules/admin/dto/admin-reset-password.dto.ts` (NEW) |
| Backend service | `backend/src/modules/user/user.service.ts` (+spec) |
| Backend controller | `backend/src/modules/admin/users-admin.controller.ts` (+spec) |
| Backend org-scope | `backend/src/modules/production-order/production-order.controller.ts` |
| Frontend page | `frontend/src/app/pages/admin/users-admin.page.ts` (+spec) |
| Frontend dialogs | `frontend/src/app/pages/admin/user-form-dialog.component.ts`, `reset-password-dialog.component.ts` (NEW) |

## Known limitations (non-blockers)

- Live Mongo e2e not run (no Mongo in sandbox) — unit suites + typechecks only, per task constraints.
- `users-admin.page.ts` + `roles-admin.page.ts` carry pre-existing `no-raw-http-in-components` eslint warnings (TZ-232.J scope, explicitly out of TZ-257.A.1 scope).

## Cross-references

- Lock: `.mimocode/locks/TZ-257.A.1-admin-user-mutations.lock`
- STATUS.md rows updated: TZ-247 idempotency, TZ-238-241 org scoping, TZ-257.A moved to DONE.

ARCHIVE_MARKER
outcome: DONE
closed_at: 2026-08-01
closed_by: autonomous-agent (Codebuff session)
source_task: tasks/TZ-257.A.1-follow-up.md
protected_files:
  - backend/src/common/guards/last-admin.guard.ts
  - backend/src/modules/admin/users-admin.controller.ts
  - backend/src/modules/user/user.service.ts
  - frontend/src/app/pages/admin/users-admin.page.ts
affected_files: 12 (see Files shipped)
acceptance_status: ALL SECTIONS DONE
verification: tsc 0/0 + backend 238/238 + frontend 559/559 + users-admin 5/5 + eslint 0 errors + diff --check clean
review: APPLIED (code-reviewer-deepseek-flash, multiple rounds)
lock_file: .mimocode/locks/TZ-257.A.1-admin-user-mutations.lock
successor_required: TRUE (TZ-257.B — roles CRUD + DTO whitelist; see TZ-260 handoff)
