# TZ-154 — Миграция legacy HttpClient → httpResource (6 страниц) — ORPHANED + SUPERSEDED

ARCHIVE_MARKER
outcome: ORPHANED
closed_at: 2026-08-01
closed_by: autonomous-frontend-finalizer (Phase 0, frontend wave 2)
related_batch: tasks/_archive/2026-08/frontend-wave2-orphan-batch-2026-08-01.md
lock_file_skipped: TRUE
deleted_from_active: N/A (нет task-файла для удаления)
supersedes_realization: TZ-232 Wave C-D + TZ-232.I ESLint rule

---

## 1. Состояние исходного task-файла

`ls tasks/TZ-154*` → "cannot access: No such file or directory".
Файл **НЕ существует**. Задача присутствует только в STATUS.md (line 394) как запись:
> TZ-154 | Миграция legacy HttpClient → httpResource (6 страниц) | 4-6h | —

Никаких acceptance criteria, conflict keys или TZ-235 dependency graph не определено.

## 2. SUPERSEDED — фактическая замена

Оригинальный scope TZ-154 (миграция 6 страниц с raw HttpClient на httpResource) реализован через:

1. **TZ-232 Wave A (lookup-table rewrite)** + **TZ-232.B (`defineEntity`)** + TZ-232.N (SubmitGuard/Idempotency) — все используют `silent-http` helpers, НЕ raw HttpClient.
2. **TZ-232 Wave C-D** мигрирует 6 list-pages на `<pi-entity-list>` (signal-based, httpResource baked-in).
3. **TZ-232.I** (✅ DONE 2026-08-01, archived by current agent) реализует ESLint rule `kppdf-frontend-architecture/no-raw-http-in-components` который ЛИТ-УРОВЕНЬ блокирует `import { HttpClient } from '@angular/common/http'` и `this.http.<verb>(...)` в `*.page.ts` + `*.component.ts`. См. `frontend/eslint/rules/no-raw-http-in-components.cjs` + спецификации в `frontend/eslint/rules/no-raw-http-in-components.spec.cjs`.

## 3. Аудит Phase 0 (2026-08-01)

`grep 'inject(HttpClient)\|this\.http\.\(get\|post\|put\|patch\|delete\)' frontend/src/app/**/*.page.ts,*.component.ts` → **0 matches** в production pages/components.

Проверено 16 page files (orders, contracts, products, materials, organizations, work-types, modules, dictionary, units, stock-movements, storage-items, inventory-dashboard, texts, tables, templates) — все используют либо `httpResource` напрямую, либо `defineEntity()` shim, либо page-owned service через `inject()`.

`grep 'httpResource' frontend/src/app/**/*.ts` → 71 matches. Pattern уже стандартизирован.

`as any` в production page/component → 2 matches оба в `capability-route-guard.spec.ts` (test spec, не production).

## 4. Acceptance criteria

N/A per Phase 1 protocol — нет исходного ТЗ, не придумываем критерии.
- Реальное состояние кода: scope TZ-154 полностью покрыт predecessor'ами и successors.
- Smoke test: `pnpm lint` exit 0 (20 NEW warnings from TZ-232.I rules = proof of enforcement).
- Регрессии: 5 PRE-EXISTING failed jest suites (capabilities/storage-items/forbidden/dsl-entity/capability-route.guard) — out of scope этого archive.

## 5. Successor plan

**Не требуется**. Все acceptance из оригинальной STATUS.md записи покрыты:
1. ESLint enforcement (TZ-232.I ✅).
2. Page migration (TZ-232 Wave C-D).
3. Service abstraction (silent-http + defineEntity).

Если в будущем потребуется мигрировать 1-2 legacy service-level usages, создать TZ-154.1 (targeted).

## 6. Conflict keys

N/A — этот archive не имеет реальных changes.

## 7. Code review

N/A — нет code changes в этом archive (audit-only).

## 8. Known limitations (carried by this archive)

- Phase 0 не выполняет браузерный smoke test всех 6 бывших TZ-154 страниц — пропущен как out-of-scope сужаемого audit-batch.
- Конфликт lock с TZ-110..127 backend batch (pre-existing 82 verify-status.sh discrepancies, не вызван этим archive).
