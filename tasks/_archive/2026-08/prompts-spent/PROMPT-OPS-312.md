# Prompt — TZ-OPS-312 (catalog specs dictionary-labels flush)

CLAIM первым (до кода), канон `D:\kppdf-8.0` (Freebuff worktree OK **только** если итог на `origin/main`):
1) Get-Location + git rev-parse → workspace
2) `tasks/_active/TZ-OPS-312.md` (копия TZ) + checklist по `docs/agent-checklists/_TEMPLATE.md`
3) Status CLAIMED; Claim slot: agent_id + claimed_at (ISO) + workspace
4) `_active-map` + чужие `_active` keys → конфликт = STOP
5) Team Room claim best-effort

Затем прочитай и выполни:
`tasks/_backlog/ops/TZ-OPS-312-catalog-page-specs-dictionary-labels-flush.md`

Только specs. Deploy НЕ. После gates — archive + commit/push на origin/main (или отчёт Cursor для land, если протокол freebuff запрещает push).
