# PROMPT — Freebuff: большая очередь, всё что сейчас готово, по порядку

> Собрано из: desktop-волны, аудита. Уже DONE (не включено сюда):
> TZD-59, TZD-61, TZD-57, TZ-PARTY-305. В работе прямо сейчас (не трогать,
> claim'ит другой агент): TZ-ORDERS-307.
> Deploy / wipe / prod Synology / установка cron на сам сервер — ЗАПРЕЩЕНЫ
> без явного слова PO в чате.

Скопируй **весь блок ниже** в новый чат Freebuff и начни немедленно. Иди по
очереди до конца, не жди PO между задачами (кроме явных СТОП-условий внутри).

---

```text
Ты — executor kppdf-8.0. Репо: D:\kppdf-8.0
Прочитай: GEMINI.md + .agents/skills/kppdf-executor-loop/SKILL.md + этот файл.

Deploy / wipe / prod Synology — ЗАПРЕЩЕНЫ без явного слова PO в чате.
Установка cron на прод-сервер (TZ-OPS-318 ШАГ 2) — только если есть SSH-доступ
И PO явно сказал «настрой на сервере»; иначе — только подготовить строку в доке.
Push — можно (GitHub только хранилище). Изменения в .github/ — ЗАПРЕЩЕНЫ
(решение PO 2026-08-21: GitHub только как хранилище, без Actions).

═══ ПЕРЕД СТАРТОМ ═══
Прочитай tasks/_active/ — если там что-то есть (например TZ-ORDERS-307), это
чужая работа, не трогай эти файлы. Всё в очереди ниже с ними не пересекается,
кроме последнего пункта (TZ-OPS-317) — тот подожди, пока _active не станет пуст.

═══ ЦИКЛ (на каждый TZ) ═══
1. CLAIM: файл TZ → tasks/_active/, checklist по _TEMPLATE.md, Claim slot.
2. Код строго по Scope/ЧТО ДЕЛАТЬ из TZ. Не трогать файлы вне списка.
3. Гейты из TZ — все PASS перед archive.
4. Archive → tasks/_archive/2026-08/<ID>.done.md + lock + commit + push.
5. Обнови docs/agent-checklists/_NOW.md одной строкой.
6. Сразу следующий TZ по очереди — не жди PO.

═══ ОЧЕРЕДЬ (по порядку, каждый следующий — независимо от предыдущего) ═══

1. tasks/TZD-60-desktop-installer-errors-hardening.md
   Требует реальной установки Desktop на Windows (ШАГ 1 внутри TZ). Если такой
   возможности нет в этом чате — не выдумывай фикс по гипотезе, archive как
   .deferred.md с причиной, иди дальше по очереди.

2. tasks/TZ-CORE-302-soft-delete-coverage-gap.md
   Backend, 61 схема без deletedAt/opt-out — решение по каждой, не одной правкой.

3. tasks/TZ-OPS-318-automated-backup-rotation.md
   Ротация бэкапов (backup.sh) + документация cron-строки. Саму установку на
   сервер — только по правилу из «ПЕРЕД СТАРТОМ» выше.

4. tasks/TZ-OPS-319-local-pre-push-gate.md
   Новый .husky/pre-push (typecheck backend+frontend). НЕ трогать .github/.

5. tasks/TZ-OPS-317-git-line-endings-normalize.md
   ПОСЛЕДНИЙ. Один коммit на весь репозиторий. СТАРТ ТОЛЬКО когда tasks/_active/
   полностью пуст (в т.ч. TZ-ORDERS-307 из другого чата уже archived) — проверь
   перед claim, если не пуст — подожди/пропусти и напиши в _NOW почему.

═══ СТОП (общий, для любого TZ в очереди) ═══
- Conflict keys пересеклись с чужим _active → DEFER, запись в _NOW, следующий TZ.
- Gates FAIL после 2 попыток → archive .failed.md, запись в _NOW, не деплоить.
- Просит деплой/wipe/GitHub Actions/установку на прод без явного слова PO → не делай.

═══ DoD волны ═══
В конце — таблица по всем 5 (или меньше, если что-то DEFER/FAILED):
ID | outcome | archive path | SHA | gates. Без «волна закрыта» — только per-TZ.
```

---

**Тексты TZD-60, TZ-CORE-302, TZ-OPS-318, TZ-OPS-319, TZ-OPS-317** — уже лежат
в `tasks/` (присланы отдельными файлами ранее в этой сессии), Freebuff читает
их напрямую по путям из очереди — копировать никуда не нужно.

**Два решения, принятые за PO в этой сессии (озвучь, если Freebuff спросит):**
- Облачная проверка кода (GitHub Actions) — НЕ включаем, вместо неё TZ-OPS-319
  (локальная проверка перед push). Решение основано на уже существующей
  политике PO от 2026-08-21.
- Бэкапы — нужны, автоматические, с очисткой старых копий (TZ-OPS-318).
  Снимки VM у PO уже есть отдельно — это дополнительная, более гибкая защита
  поверх них, не замена.
