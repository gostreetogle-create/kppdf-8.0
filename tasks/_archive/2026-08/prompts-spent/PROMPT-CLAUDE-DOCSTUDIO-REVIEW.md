# PROMPT — Cursor (обычная модель) + MCP `claude_code` · ревизия плана студии документов

Analysis-only. Проверяет **уже написанный** план `docs/architecture/nx-doc-studio.md` до того, как
по нему пойдут срезы S2–S8. Пишет один файл `docs/adr/ADR-NX-DOC-STUDIO-REVIEW.md`, поэтому
безопасно идёт параллельно с обеими волнами Freebuff.

```text
Ты — независимый архитектор-ревьюер kppdf-8.0. Режим ANALYSIS-ONLY: не менять
ни один файл, кроме единственного отчёта (путь в конце). Не писать код продукта,
не создавать TZ, не деплоить. Развилки прогонять через MCP claude_code
analysis-only.

ПРЕДМЕТ РЕВИЗИИ: docs/architecture/nx-doc-studio.md — план объединения трёх
legacy-поверхностей (Document Studio, Builder, КП Workspace) в одну страницу
студии документов в NX. Твоя задача не переписать план, а найти в нём то, что
сломается на реализации.

Читать: docs/architecture/nx-doc-studio.md (предмет) ;
docs/architecture/document-studio.md (ADR legacy, ACCEPTED v1.2) ;
docs/architecture/document-studio-data-anchors.md ;
docs/pages/kp-workspace-geometry.md (закон геометрии) ;
docs/pages/{document-studio,builder,kp-workspace}.page.md ;
docs/PO-CANON.md ; docs/CONTEXT.md ;
backend/src/modules/{document-template,template-block,studio-document,
document-render,generated-document,table-template,text-block,doc-type,
registry}/** ;
frontend/src/app/pages/doc-constructor/** (Studio + Builder) ;
frontend/src/app/pages/commercial/proposals/workspace/** ;
frontend-nx/libs/ui/paper-and-ink/src/lib/{canvas,rich-text}/** ;
frontend-nx/apps/kppdf-web/src/app/layout/**

Дать вердикт ПОДТВЕРЖДАЮ / ВОЗРАЖАЮ / РИСК по каждому пункту, с file:line:

1) § 3 «берём как есть» — каждая строка таблицы действительно существует в
   backend с указанным полем/роутом? Есть ли что-то, что план объявил готовым,
   а оно на деле полуготово (например работает только для КП, а не для любого
   типа документа)?

2) § 4 дельта D1 (поля типографики на блоке) — согласен ли ты с гибридом
   «поля на блоке + HTML внутри абзаца», или это заведёт в двойной источник
   правды для стиля? Как серверный рендер должен разрешать конфликт, когда
   блочный шрифт говорит одно, а inline-HTML другое? Есть ли путь дешевле.

3) § 4 дельта D2 (pageMargins) и D3 (catalog-products как источник строк) —
   достаточно ли этого, чтобы «позиции из каталога с пересчётом» реально
   работали? Кто считает итоги: сервер или клиент, и что тогда с числами в
   PDF против экрана. Назови конкретные поля/файлы, которых не хватает.

4) § 4 D4 (снос document_table_types) — подтверди отсутствие потребителей
   grep'ом или возрази.

5) § 5 геометрия — план утверждает, что плавающий контекстный тулбар
   типографики решает требование PO «меню не сужают лист» в альбомной
   ориентации. Проверь против закона kp-workspace-geometry.md: не нарушает ли
   overlay-тулбар пункты 1, 4, 7. Отдельно: план переносит контрол ориентации
   с редактора шаблона на страницу студии (уточнение п.6 закона по указанию
   PO) — какие последствия для template.orientation vs studio-документа,
   созданного из шаблона с другой ориентацией.

6) § 6 порядок срезов S0–S8 — верна ли последовательность и границы? Что из
   S3–S8 обязано делаться раньше, иначе придётся переделывать? Особое
   внимание: autosave + контракт конфликта ревизий (StudioDocument.revision,
   409 STUDIO_DOCUMENT_REVISION_CONFLICT) появляется в S3 — не поздно ли, и
   не рано ли рисовать блоки до решения по типографике.

7) § 8 риски — чего в списке не хватает? Отдельно оцени: template_blocks
   dual-read cutover (шаги 5–6 не закрыты) и DocumentTemplateService как
   god-object — как новые срезы обязаны их обходить, чтобы не углубить долг.

8) Единая поверхность против трёх: план оставляет legacy Studio/Builder/КП
   работать до отдельного kill-plan PO. Какие конкретные конфликты данных
   возможны, если оператор правит один и тот же studio_document и в legacy,
   и в NX? Нужен ли на срезе S2 явный признак «документ создан в NX».

Вывод: docs/adr/ADR-NX-DOC-STUDIO-REVIEW.md, ≤120 строк:
- ПОДТВЕРЖДАЮ (пункты плана, устоявшие проверку кодом)
- ВОЗРАЖАЮ (пункт плана → чем заменить → обоснование file:line)
- РИСКИ, не покрытые планом
- ПОРЯДОК: предлагаемые правки последовательности S0–S8, если нужны
- ВОПРОСЫ PO (только необратимое: схема, ownership, удаление сущности) —
  формат «варианты A/B + рекомендация + цена ошибки», не более 3 вопросов

Не предлагать другие модули переноса и не переставлять приоритеты PO
(PO-CANON п.7). Склад не проектировать. Коммитить только этот файл.
```
