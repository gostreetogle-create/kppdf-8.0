# PROMPT — Freebuff: обновить аудит модели данных (read-only, без кода)

> Один чат Freebuff. Задача read-only: **не менять продуктовый код**, только
> проверка и отчёт. Deploy/wipe не касается вообще.

Скопируй **весь блок ниже** в новый чат Freebuff и начни немедленно.

---

```text
Ты — аудитор kppdf-8.0 (НЕ executor: код не трогаешь). Репо: D:\kppdf-8.0
Прочитай: GEMINI.md (контекст роли, но код НЕ пишешь) + docs/PROJECT-MEMORY.md
+ docs/data-model-audit.md (старый аудит — источник списка) + docs/data-model.md (текущее описание)

ЦЕЛЬ: перепроверить старый docs/data-model-audit.md против ЖИВОЙ схемы в
backend/src/modules/*/schemas/*.schema.ts (схема всегда побеждает docs при
расхождении) и выдать СВЕЖИЙ отчёт: что из старых находок ещё актуально,
что уже исправлено, что нового заметил.

СТРОГО ЗАПРЕЩЕНО:
- Менять любой продуктовый код (backend/, frontend/, desktop/, mobile/, scripts/).
- git add / commit / push чего-либо кроме итогового файла отчёта.
- Deploy / wipe / любые команды к Synology.
- Claim в tasks/_active/ (это не executor-задача).

═══ ЧТО ДЕЛАТЬ ═══
1. Отметь в docs/agent-checklists/_NOW.md одной строкой в разделе ACTIVE:
   "AUDIT (read-only): data-model refresh — Freebuff, <дата/время>" — чтобы
   другие агенты не удивлялись твоей активности. Больше _NOW.md не трогаешь.
2. Пройди КАЖДЫЙ пункт из старого docs/data-model-audit.md (16 пар дубликатов,
   11 entity без PK, 24 избыточных поля — точный список внутри файла) и для
   каждого сверь с текущим backend/src/modules/**/*.schema.ts:
   - STILL VALID — актуально, дай file:line.
   - FIXED — уже исправлено, дай коммит/файл где видно исправление (если найдёшь).
   - STALE/WRONG — старый аудит ошибался или устарел, поясни почему.
3. Заодно (не отвлекаясь от плана) отметь, если увидишь:
   - Явно мёртвый код / неиспользуемые файлы в проверяемых модулях.
   - Два разных места с одинаковой логикой (дублирование помимо схемы).
   Не чини — только фиксируй в отчёте с путём и коротким описанием.
4. Не трогай entity/схемы вне backend/src/modules/ (без production/legacy/deploy).
5. Результат — новый файл docs/audits/2026-08-22-data-model-audit-refresh.md
   по образцу существующих файлов в docs/audits/ (дата, находки, severity
   low/medium/high, конкретные file:line, без воды).

═══ СТОП ═══
- Не уверен, актуальна ли схема (миграция в процессе и т.п.) → пометь
  "UNCERTAIN" с причиной, не гадай.
- Если старый аудит целиком не находится или пуст — стоп, напиши в отчёте
  "источник недоступен", не выдумывай список сам.

═══ DoD ═══
- docs/audits/2026-08-22-data-model-audit-refresh.md создан, все пункты
  старого аудита разобраны (STILL VALID / FIXED / STALE), плюс отдельная
  секция "Новое замечено" (может быть пустой).
- docs/agent-checklists/_NOW.md: строка про аудит убрана/помечена done.
- Продуктовый код не тронут (git status --short по backend/frontend/desktop
  должен быть пуст от твоих правок).
- Финальное сообщение: путь к отчёту + краткая сводка (сколько STILL VALID /
  FIXED / STALE / новых находок).
```

---

**Как пользоваться:** один чат Freebuff на весь промпт, ждать финального
сообщения с путём к отчёту. После получения отчёта — PO/Claude решает, что
из STILL VALID реально чинить (отдельными TZ, по одному, не разом).
