═══════════════════════════════════════════════════════════════
TZD-54: Desktop — три двери UI (Подключение | Импорт | AI)
═══════════════════════════════════════════════════════════════

> Канон: `docs/audits/2026-08-16-desktop-ia-shell-proposal.md`
> Wave: `tasks/_backlog/desktop/WAVE-DESKTOP-IA-SHELL.md`
> PO: доверил раскладку «как архитектор считает» → 3 двери.
> **Деплой запрещён** в этом TZ (VPN / отдельное слово PO).

РОЛЬ АГЕНТА: Desktop UI (Svelte App.svelte + тонкий CSS). Логику импорта/MCP/модели не переписывать — только IA shell.

ЗАВИСИМОСТИ: TZD-53 code DONE (`ac7a49ed`, 0.5.5). Closeout TZD-53 желателен до старта (не блокер кода). TZD-55 не параллелить (тот же App.svelte).

LAYER: 3

CONFLICT KEYS: `desktop/src/App.svelte` ; `desktop/docs/MCP.md` ; `desktop/README.md` ; `docs/agent-checklists/TZD-54.md` ; `tasks/_backlog/desktop/WAVE-DESKTOP-IA-SHELL.md`

PAGES: N/A
PAGE_DOCS: N/A

STATUS: READY

═══════════════════════════════════════════════════════════════
ИСХОДНОЕ
═══════════════════════════════════════════════════════════════

Проверено:
- Top tabs сейчас: `import` | `mcp` | `model` (подписи «Студия импорта» / «MCP» / «Модель»).
- На вкладке `mcp` уже смешаны паринг («Подключение») и MCP host — путаница имён.
- Form Studio + drop + inbox живут на `import`.
- Локальная модель — отдельная вкладка `model`.
- Hint-система (`HINTS` + showHint) уже есть — расширить, не ломать.

═══════════════════════════════════════════════════════════════
ЧТО ДЕЛАТЬ
═══════════════════════════════════════════════════════════════

ШАГ 1: Tab model

  1.1. Тип вкладок: `'connection' | 'import' | 'ai'` (заменить `mcp`/`model`).
  1.2. Подписи RU строго: **Подключение** | **Импорт** | **AI**.
  1.3. `data-test`: `tab-connection`, `tab-import`, `tab-ai` (старые `tab-mcp`/`tab-model` — удалить или alias один релиз: лучше сразу новые + обновить тесты если есть).
  1.4. Default tab = `import`.
  1.5. Subtitle шапки: одна строка RU вроде «Импорт данных и локальный AI для kppdf» (не «MCP» в заголовке).

ШАГ 2: Содержимое дверей

  2.1. **Подключение** — только: вставка pairing JSON, Подключиться / Отключиться, статус connected, compat-баннер если есть. **Без** MCP host UI.
  2.2. **Импорт** — без изменений логики: Формы Excel, drop/файл, маппинг, inbox, send, отчёты. Вверху — короткая шпаргалка 3 шага (скачал форму / заполнил / загрузил) если ещё нет.
  2.3. **AI** — два визуальных блока (не вложенные вкладки, если можно плотно на одном экране):
       - **Локальная модель** — существующие controls (статус, выбор, Запустить/Скачать/Перезапустить). Добавить placeholder-кнопку «Открыть папку моделей» **disabled** с hint «появится в TZD-55» **или** сразу вызвать `defaultModelDir`+open если уже есть паттерн как inbox (предпочтительно реализовать open здесь, если 5 строк; иначе stub + TZD-55).
       - **MCP для агентов** — существующие MCP start/stop/port/copy mcp.json. Hint: «Нужен Cursor или LM Studio. Для обычного импорта Excel не обязателен.»
  2.4. Явный RU-баннер на AI (один раз): «Импорт и Excel-формы работают без модели и без MCP.»

ШАГ 3: Подсказки кнопок

  3.1. Пройти основные кнопки трёх дверей: короткие HINTS (1 предложение, по-русски, без англ. жаргона в UI).
  3.2. «Скачать Excel-форму» — уже есть; проверить что hint не требует аккаунт.

ШАГ 4: Docs + gates

  4.1. `desktop/README.md` + `desktop/docs/MCP.md` — 5–15 строк: три двери, что где.
  4.2. Gates: `cd desktop && npx tsc --noEmit` + `npx svelte-check --threshold error`.
  4.3. Не bump версии и не deploy в этом TZ (если нужен bump — только если PO позже катит вместе с 55).
  4.4. Checklist + READY FOR REVIEW; archive после Cursor PASS.

═══════════════════════════════════════════════════════════════
НЕ ИЗМЕНЯТЬ
═══════════════════════════════════════════════════════════════

- Алгоритмы маппинга / Form Studio fingerprint / createEntities / dedupe
- MCP protocol tools, ai-runner HTTP API (это TZD-55)
- frontend Angular, wipe, deploy.ps1
- Чужой WIP seeds/PO-*

═══════════════════════════════════════════════════════════════
КРИТЕРИИ ПРИЁМКИ
═══════════════════════════════════════════════════════════════

- [ ] Видны ровно 3 вкладки: Подключение | Импорт | AI
- [ ] Паринг только на Подключение; MCP host только на AI; импорт/формы только на Импорт
- [ ] На AI есть явный текст «без модели/MCP импорт работает»
- [ ] Все UI-строки вкладок/баннеров на русском
- [ ] Gates tsc + svelte-check PASS
- [ ] Нет deploy; Executor report (auto); Cursor PASS → archive

known_limitation: починка `resolveDesktopDir` / NSIS runner → TZD-55.

HANDOFF ниже в ответе PO.
