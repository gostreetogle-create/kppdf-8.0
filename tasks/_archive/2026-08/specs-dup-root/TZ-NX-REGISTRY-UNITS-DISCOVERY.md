# TZ-NX-REGISTRY-UNITS-DISCOVERY

Analysis-only discovery for the first real NX registry: Units of Measure.

The executor must create an active claim and checklist before work, inspect legacy and NX sources, and archive a verified report. No product code or configuration changes are allowed.

Required report: legacy entity/API/validation/permissions inventory, NX mapping, risks, ordered implementation plan, parallelization map, and exact blockers. Never invent fields, permissions, endpoints, or credentials. See the execution prompt supplied by the coordinator.
