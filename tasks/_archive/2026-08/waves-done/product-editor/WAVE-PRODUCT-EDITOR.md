# WAVE-PRODUCT-EDITOR — FullEditor изделия: имя + плотная вёрстка + состав

**STATUS:** DONE — TZ-PRODUCTS-308 + TZ-PRODUCTS-309
**SoT:** `D:\kppdf-8.0` на `main`  
**Промпт:** [`PROMPT-CONTINUOUS.md`](./PROMPT-CONTINUOUS.md)  
**Deploy:** только по команде PO

## Зачем (для PO)

Сейчас диалог «Редактировать продукт» длинный, поля жирные, а блок «Состав» — загадочная фраза про «профиль L». Нужно: одно понятное слово **Изделие**, плотная форма в 2–3 колонки, и **реальный состав** в том же окне (не намёки).

## Канон имён (lock)

| Где | Пишем |
|-----|--------|
| UI (диалог, заголовки, toasts, empty) | **Изделие** / «Редактировать изделие» / «Новое изделие» |
| Раздел меню / список | **Продукция** (как сейчас — полка каталога) |
| Код / API / `Product` / routes `/products` | **не переименовывать** |

Kind `good` в форме: лейбл **«Изделие»** (вместо «Товар»), `service`/`work` без смены смысла.

## Порядок

| # | TZ | После чего |
|---|-----|------------|
| 1 | **TZ-PRODUCTS-308** densify + rename UI | — |
| 2 | **TZ-PRODUCTS-309** состав в FullEditor (reuse BomPanel) | **308 DONE** |

Цикл: claim → code → gates → archive → commit+push → next. Без «поехали».

## BAN

- Не воскрешать ModuleMaterials / второй write-path состава  
- Не переименовывать коллекцию/класс `Product`  
- Не трогать QuickCreate профили S/M/L (отдельная тема)  
- deploy без команды PO  

## DoD волны

1. В диалоге нет слова «продукт» в заголовках/кнопках (кроме неизбежного URL).  
2. Форма плотная: габариты/вес/ед./цвет — узкие поля; блоки слева→направо.  
3. В режиме **редактирования** виден и работает тот же состав, что на карточке (`ProductBomPanel`).  
4. Нет текста «профиль L» / «собирается на карточке» как единственной помощи.  
