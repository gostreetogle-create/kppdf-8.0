ARCHIVE_MARKER
outcome: DONE
closed_at: 2026-08-02
closed_by: Buffy
commit: 058ff7c
verification:
  - acceptance criteria: PASS
  - frontend jest: PASS (699/699 full)
  - backend jest: PASS (320/320 full)
  - ng build (development): PASS
  - frontend typecheck: PASS (targeted; full tsc blocked by foreign categories.page.ts)
  - backend typecheck: PASS
  - git diff --check: PASS
  - code review: PASS
  - verify-status.sh: PASS
browser: MANUAL_BROWSER_CHECK_REQUIRED (no live dev-stack credentials in this session)

═══════════════════════════════════════════════════════════════
TZ-279: Workflow — устранить дублирование build-команд
═══════════════════════════════════════════════════════════════

РОЛЬ АГЕНТА: Frontend Architect / Release Manager

ЗАВИСИМОСТИ: TZ-263 (добавил `check:build` в run-project-checks)

LAYER: 1 (конфигурация/документация)

CONFLICT KEYS:
frontend/package.json;.husky/pre-commit;ARCHITECTURE.md

ПРИМЕЧАНИЕ О НОМЕРЕ: изначально задача заказана как «TZ-276 — устранить
дублирование build-команд», но номер TZ-276 уже занят файлом
`tasks/TZ-276-builder-template-dialog-autoclose.md` (другая сессия; тема
автозакрытия диалога — уже реализована в TZ-DOC-268). По правилу «не
создавай дубликаты номеров» задача переименована в TZ-279.

═══════════════════════════════════════════════════════════════
ИСХОДНОЕ СОСТОЯНИЕ
═══════════════════════════════════════════════════════════════

1. `frontend/package.json` содержит ДВА идентичных скрипта:
   - `"build:dev": "ng build --configuration development"` (строка 10)
   - `"check:build": "ng build --configuration development"` (строка 11)
2. `check:build` добавлен TZ-263 как алиас; `build:dev` существовал раньше.
3. Ссылки на `check:build`:
   - `.husky/pre-commit` (комментарий: `pnpm --dir frontend check:build`);
   - `ARCHITECTURE.md` строка ~113 (таблица Dev tooling);
   - исторические записи progress.md / TZ-263.done.md (НЕ переписывать).
4. Канонический документированный способ запуска dev build — прямая
   команда `ng build --configuration=development` (SKILL.md, AI-AGENT-GUIDE.md).
5. CI (`.github/workflows/ci.yml`, `nightly.yml`) не ссылается ни на
   `check:build`, ни на `build:dev` — конфликта с CI нет.

═══════════════════════════════════════════════════════════════
ЧТО ДЕЛАТЬ
═══════════════════════════════════════════════════════════════

ШАГ 1: Определить канонический npm-скрипт
- Канонический: `build:dev` (стандартное имя, существовало до TZ-263).

ШАГ 2: Удалить избыточный алиас
- Удалить `"check:build": "ng build --configuration development"` из
  `frontend/package.json`. Скрипт `build:dev` сохранить.

ШАГ 3: Синхронизировать документацию
- `.husky/pre-commit`: в комментарии `check:build` → `build:dev`.
- `ARCHITECTURE.md`: строка про «Алиас `pnpm --dir frontend check:build`»
  → «Алиас `pnpm --dir frontend build:dev`».
- SKILL.md и AI-AGENT-GUIDE.md уже используют прямую команду —
  не менять.
- progress.md и архивы (TZ-263.done.md) — история, не переписывать.

ШАГ 4: Проверки
- `pnpm exec ng build --configuration=development` → exit 0;
- `git diff --check` → чисто;
- grep по репозиторию: `check:build` остаётся ТОЛЬКО в исторических
  записях (progress.md, tasks/_archive/*.done.md, .freebuff/*).

═══════════════════════════════════════════════════════════════
ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ
═══════════════════════════════════════════════════════════════

ИЗМЕНЯТЬ:
- frontend/package.json (удалить строку `check:build`)
- .husky/pre-commit (комментарий)
- ARCHITECTURE.md (строка Dev tooling)

НЕ ИЗМЕНЯТЬ:
- pnpm-lock.yaml / frontend/pnpm-lock.yaml (без необходимости)
- progress.md, tasks/_archive/*.done.md (история)
- .agents/skills/run-project-checks/SKILL.md (уже прямая команда)
- docs/AI-AGENT-GUIDE.md (уже прямая команда)

═══════════════════════════════════════════════════════════════
КРИТЕРИИ ПРИЁМКИ
═══════════════════════════════════════════════════════════════

1. `frontend/package.json` содержит ровно один скрипт для dev build
   (`build:dev`); `check:build` отсутствует.
2. `.husky/pre-commit` и `ARCHITECTURE.md` ссылаются только на
   `build:dev` (или прямую команду).
3. `pnpm exec ng build --configuration=development` → exit 0.
4. `git diff --check` → exit 0.
5. Build gate сохранён (SKILL.md шаг 2 не тронут).
6. Никакие unrelated скрипты не удалены.
