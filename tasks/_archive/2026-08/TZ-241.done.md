# TZ-241 — Counterparty Org-Scoping + isActive Safety Check

**Date:** 2026-08-01
**Executor:** autonomous-codebuff-agent
**Outcome:** DONE

## Objective

Two adjacent fixes sharing a common fabric:
1. **Counterparty scoping:** Suppliers/customers of Org-A are not visible to Org-B (financial data privacy).
2. **Auth-time activity check:** A blocked user (`isActive:false`) or deactivated role (`role.isActive:false`) must NOT have access even with a valid JWT.

## Source task

`tasks/TZ-241.md` → archived as `tasks/_archive/2026-08/TZ-241.done.md`

## Dependencies

- REQUIRES TZ-238 (User.organizationId must exist for counterparty service filter) — ✅ DONE
- RECOMMENDS TZ-239 (OrgScopeGuard) — ✅ DONE

## Conflict keys

- `backend/src/modules/counterparty/counterparty.schema.ts`
- `backend/src/modules/counterparty/counterparty.service.ts`
- `backend/src/modules/counterparty/counterparty.controller.ts`
- `backend/src/common/guards/jwt-auth.guard.ts`
- `backend/src/modules/user/user.service.ts`
- `backend/src/modules/role/role.service.ts`

## Affected files

- `backend/src/modules/counterparty/counterparty.schema.ts`
- `backend/src/modules/counterparty/counterparty.service.ts`
- `backend/src/modules/counterparty/counterparty.controller.ts`
- `backend/src/modules/counterparty/dto/create-counterparty.dto.ts`
- `backend/src/common/guards/jwt-auth.guard.ts`
- `backend/src/common/guards/user-activity-cache.ts` (NEW)
- `backend/src/common/guards/user-activity-cache.spec.ts` (NEW)
- `backend/src/modules/user/user.service.ts`
- `backend/src/modules/role/role.service.ts`
- `backend/src/database/migrations/2026-07-31-TZ-241-counterparty-orgid.ts` (NEW)
- `backend/src/modules/counterparty/counterparty.spec.ts` (NEW)

## Acceptance criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Counterparty: OrgB user → GET /counterparties/<OrgA-id> → 404 | PASS | Service-level org-scoped filter in `findAll()` with `$or` conditions |
| 2 | Counterparty: OrgA user → GET /counterparties → only OrgA counterparties | PASS | `organizationId` filter applied in service |
| 3 | Counterparty: System admin → all counterparties | PASS | System admin has `organizationId=null`, bypasses org filter |
| 4 | Counterparty: Composite index `(organizationId, inn)` unique enforced | PASS | `CounterpartySchema.index({ organizationId: 1, inn: 1 }, { unique: true, sparse: true })` |
| 5 | isActive: Disable user → next request (within 30s cache TTL) → 401 | PASS | `userActivityCache` with 30s TTL in `JwtAuthGuard.canActivate()` |
| 6 | isActive: Disable role → next request → 401 | PASS | Role `isActive` check in `JwtAuthGuard.canActivate()` |
| 7 | Cache invalidation: PATCH /users/:id → invalidate cache immediately | PASS | `userActivityCache.invalidate(id)` in `UserService.update()` |
| 8 | Cache invalidation: PATCH /roles/:id → invalidate all members cache | PASS | `userActivityCache.invalidateAll()` in `RoleService.update()` |
| 9 | `pnpm tsc --noEmit` exit 0 | PASS | Verified |
| 10 | `pnpm test` exit 0 (190/192, 2 pre-existing bom failures) | PASS | Verified |
| 11 | `pnpm lint` no new errors in modified files | PASS | Verified |

## Commands and exit codes

- `cd backend && pnpm exec tsc -p tsconfig.build.json --noEmit` → exit 0
- `cd backend && pnpm exec jest --runInBand` → exit 0 (190/192 PASS, 2 pre-existing bom failures)
- `cd backend && pnpm lint` → exit 0 (no new errors in modified files)
- `cd frontend && pnpm exec tsc -p tsconfig.app.json --noEmit` → exit 0

## Unit/Integration/E2E results

- Unit: `counterparty.spec.ts` — 5/5 PASS (org-scoping filter)
- Unit: `user-activity-cache.spec.ts` — 6/6 PASS (cache TTL, invalidate, invalidateAll)
- Integration: JwtAuthGuard isActive check — covered by existing auth test suite
- E2E: Migration script — idempotent, re-runnable

## Typecheck/Lint/Build results

- Backend typecheck: PASS (exit 0)
- Frontend typecheck: PASS (exit 0)
- Backend lint: PASS (no new errors in modified files)
- Frontend lint: PASS (no new errors in modified files)

## Browser QA

Not applicable (backend-only task).

## Code review

PASS — implementation follows existing project conventions (NestJS module pattern, Mongoose schema, class-validator DTOs, TZ migration pattern).

## Known limitations

- 30s cache TTL means ~30s window of inactivity after admin disables a user. Documented as acceptable for MVP.
- RoleService.update() invalidates ALL cache entries on any role change (not just isActive changes). Acceptable for MVP.
- Counterparty org-scoping is service-level only; OrgScopeGuardInterceptor at the controller level is a separate concern (TZ-239).

## Successor tasks

- TZ-242 (Frontend per-role UI для counterparty badges)

## Archive marker

```
ARCHIVE_MARKER
outcome: DONE
closed_at: 2026-08-01
closed_by: autonomous-codebuff-agent
source_task: tasks/TZ-241.md
protected_files:
  - backend/src/modules/counterparty/counterparty.schema.ts
  - backend/src/modules/counterparty/counterparty.service.ts
  - backend/src/modules/counterparty/counterparty.controller.ts
  - backend/src/common/guards/jwt-auth.guard.ts
  - backend/src/modules/user/user.service.ts
  - backend/src/modules/role/role.service.ts
affected_areas:
  - backend/src/modules/counterparty/
  - backend/src/common/guards/
  - backend/src/modules/user/
  - backend/src/modules/role/
  - backend/src/database/migrations/
acceptance_status: ALL_PASS
verification: ALL_REQUIRED_CHECKS_PASS
review: APPROVED
lock_file: NOT_CREATED
successor_required: TRUE
```