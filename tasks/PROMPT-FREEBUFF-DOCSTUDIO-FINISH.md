# PROMPT — Freebuff Doc Studio FINISH (session bootstrap)

Используй **только если** PO хочет один master на всю сессию.  
Предпочтительно: Cursor выдаёт `tasks/_ready/docstudio-finish/prompts/PROMPT-NN-*.md` **по одному**.

Если master: читай `tasks/_ready/docstudio-finish/INDEX.md`, выполняй TZ **строго по порядку** 01→11, между ними `nx build kppdf-web` green, `/clear` между TZ по желанию.

Не параллель. Не legacy frontend. Не deploy.
