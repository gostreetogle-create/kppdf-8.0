# Данные с КП3 (локальный staging)

Сюда исполнитель **TZ-MIG-301** кладёт выгрузку с `root@130.49.129.240` (`/opt/kppdf` + Mongo `kp-app`).

- Дампы JSON / media — **не в git** (см. `.gitignore`).
- В git только этот README и отчёты в `docs/audits/`.
- Канон доступа SSH: `docs/ops/kp3-data-copy-access.md`
- Волна: `tasks/_backlog/migrate-kp3/WAVE-KP3-DATA-MIGRATE.md`
