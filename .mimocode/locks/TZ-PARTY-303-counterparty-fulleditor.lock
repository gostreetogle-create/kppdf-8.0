# Lock file
Locked as of 2026-08-08 — TZ-PARTY-303: Counterparty FullEditor + list CRUD.

Owner: agent-3e757640b7 (Cursor executor)
Protected files:
- frontend/src/app/pages/counterparties/counterparty-full-editor-dialog.component.ts
- frontend/src/app/pages/counterparties/counterparty-full-editor-dialog.component.spec.ts
- frontend/src/app/pages/counterparties/counterparties.page.ts
- docs/pages/counterparties.page.md

Invariants a successor TZ must not silently break:
- Client never sends `organizationId` / `isSystem`: the server stamps the tenant.
- Dialog stays kind C: variant="content" + min(1120px, calc(100vw - 2rem)).
- `roles` is required and non-empty (create DTO); role labels come from the catalog.
- Payload omits empty strings (API runs forbidNonWhitelisted).
- Delete is soft on the server; the page must not promise hard delete in copy.

Unlock: only via orchestrator approval (TZ-INN-301 adds the INN lookup here).
Deploy: NO
