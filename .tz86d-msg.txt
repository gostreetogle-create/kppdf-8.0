feat(frontend): TZ-86 Phase D.1 — builder canvas 3-pane (ГЛАВНЫЙ WOW)

12 files / +1822 / -0

Components:
- BuilderPage (3-pane shell orchestrator, ~310 LoC)
- BuilderToolPane (4 accordion sections: Блоки/Тексты/Таблицы/Данные, ~250 LoC)
- BuilderCanvas (cdkDropList center, ~115 LoC)
- BlockRenderer (cdkDrag leaf, 5 type renderings, ~190 LoC)
- BuilderInspector (signal-bound form, ~270 LoC)

Primitives:
- PiCanvasPage (A4 paper wrapper, OKLCH paper + hairline)
- PiCanvasBlockHandle (cdkDragHandle GripVertical, hover-only)

Service:
- TemplateBlocksService (6 silent-http methods: listByTemplate/findById/add/reorder/update/remove)

Types:
- template-block.types.ts — TS mirror of backend TemplateBlock + DataBinding (5 block types, 9 binding sources, 4 formats)

Util:
- move-item-in-array.ts — local re-implementation of CDK's internal helper

Routes:
- /doc-constructor/builder (picker state)
- /doc-constructor/builder/:id (3-pane canvas)

Nav:
- 4th NAV_CATEGORY «Документы» (FileText icon) — Конструктор / Текстовые блоки / Шаблоны таблиц

Architecture:
- Auto-save: Subject<{_id, patch}> → groupBy(_id) → debounceTime(1500) → switchMap → service.update()
  (per-block debounce so editing block A doesn't reset block B's timer)
- Reorder: optimistic UI + atomic POST /reorder + rollback on error
- Add block: optimistic insert (crypto.randomUUID() tempId) + POST add() + swap tempId for server _id
- Empty state: httpResource<DocumentTemplate[]> + pi-select dropdown → navigate to /builder/:id
- Error toast strategy: 409 → «Конфликт: шаблон изменён другим пользователем»; 500/network → «Ошибка сохранения: …»; initial load → silent fail

Pattern fidelity: Angular 20 standalone + OnPush + signals + inject() + httpResource + silent-http.

TS debugging arc (recorded in TZ-86.checklist.md for archival):
1. Path depth (root cause) — 4 files 3 levels deep, ../../shared/ → ../../../shared/
2. httpResource <DocumentTemplate[]> (not DocumentTemplateListResponse) — raw endpoint returns array
3. PI_DIALOG_DATA token (not PI_DIALOG_REF.data) — ref interface has no .data
4. CdkDropListDropped → CdkDragDrop<TemplateBlock[]> — actual CDK event type
5. CSS backticks in styles template literal (closes the template prematurely)
6. extractErrorMessage runtime null guards (httpResource.error() may be null)
7. handleSaveResult + onAddBlock early-return on !res.ok (TS2339 union narrowing)
8. _BlockTypeRef anti-pattern removal, parts: string[] annotation
