# Инструкция для bridge server (box-946037)
# Выполни эти команды на сервере 193.222.62.240:

# 1. Проверить что слушает на :4200
ss -tlnp | grep :4200

# 2. Проверить can ли VPS достучаться до VM
curl -sf http://192.168.1.103:3000/api/health

# 3. Если VM недоступна — попробовать внешний IP VM
curl -sf http://46.158.58.183:3000/api/health

# 4. Показать полный nginx config
cat /etc/nginx/sites-enabled/*

# 5. Показать что работает на :4200
pm2 list 2>/dev/null || systemctl list-units | grep -i kppdf

# 6. Проверить есть ли tunnel/tunneling
ps aux | grep -E "frp|ngrok|cloudflare|tunnel"
